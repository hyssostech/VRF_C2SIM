{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.Hosting.Lifetime": "Information"
    }
  },
  "C2SIM": {
    "SubmitterId": "VRF_C2SIM",
    "RestUrl": "http://127.0.0.1:8080/C2SIMServer",
    "RestPassword": "v0lgenau",
    "StompUrl": "http://127.0.0.1:61613/topic/C2SIM",
    "Protocol": "SISO-STD-C2SIM",
    "ProtocolVersion": "CWIX2024v1.0.2",
    "StompHeartBeat": 10000
  },
  "Vrf": {
    "Protocol": "Hla1516e",
    "ApplicationNumber": 3201,
    "SiteId": 1,
    "SessionId": 1,
    "HostInetAddr": "127.0.0.1",
    "Federation": "CWIX-2024",
    "FedFileName": "RPR_FOM_v2.0_1516-2010.xml",
    "FomModules": [ "MAK-VRFExt-6_evolved.xml", "MAK-DIGuy-7_evolved.xml", "MAK-LgrControl-2_evolved.xml" ],
    "ClientId": "STP",

    "TypeMappingMode": "RealTemplates",
    "FriendlyNation": "USA",
    "OpposingNation": "RUS",
    "TypeMapFile": "data/unit-type-map.json",
    "SurfaceProxySubstitutions": true,

    "ComposeHierarchy": true,
    "ObjectConsoleNotifyLevel": -1,

    "_DeStackCreates": "C14, USER RULING 2026-09-07 ('bite my tongue and accept trying to space out the entities at startup'), which SUPERSEDES the R8 ruling of 2026-07-13 for 5.2. Units that share ONE coordinate - STP puts a whole COA on its assembly point - are spread onto deterministic hex rings at init, first unit kept in place. 700 m is larger than the longest shipped company formation (630 m), so no two units' formations can overlap whatever their heading. WHY IT IS HERE AND NOT ONLY IN appsettings.Demo.json (added 2026-09-20): scripts\\RunC2SimScenario.ps1 sets no DOTNET_ENVIRONMENT, so a RUNNER-LAUNCHED app never loads the Demo overlay and every runner run since the ruling was made has piled its co-located units - measured on COA-STP1_Initialization.xml, 72 of 128 units sit on 10 shared coordinates. Evidence for the value: PREREG_ASSEMBLY_LAYOUT_2026-09-07 ('the spacing ruling (C14) is CONFIRMED on every prediction'). It moves units off their authored positions, so it is NOT the C# default (VrfSettings.DeStackCreates stays false, and the byte-parity golden-trace path is unaffected); a run that must reproduce authored coordinates sets Vrf__DeStackCreates=false. SCOPE (narrowed 2026-09-20, cold-start review SF2): only units created as INDEPENDENT VR-Forces objects are spread. A unit that Vrf:ComposeHierarchy attaches INTO a parent aggregate takes its place from the PARENT'S FORMATION (UG52 25.2.1), so it is never displaced and never occupies a ring slot - C14's own argument is that two SIBLING units' 630 m formations must not overlap, which says nothing about a unit and its own parent, and a coordinate a unit holds only because InitParser cascaded its superior's onto it (InitParser.cs:144-153) is not an authored co-location at all. MEASURED, not argued (--destack-selftest section 11, which parses the real files in BOTH Vrf:TypeMappingMode values): R9_Mojave_Lean_Initialization.xml 0 groups / ZERO units moved (3 platoons are taskee 114.MechCoy's own composed members and stay on its coordinate; before this rule they were composed into the company from 700 m away), R9_Mojave_Initialization.xml 0 / 0 (43 composed children held), COA-STP1_Initialization.xml 10 groups / 62 moved and STP-IRON-STORM-SYNTHETIC_Initialization.xml 2 / 12 - both UNCHANGED, because COA-STP1 declares no Superior at all and Iron Storm's two declared parents (28ID, III Corps) map to platforms, which compose nothing. So the R9 rehearsal is a no-op under de-stack and stays comparable with the D1/D1b/D3 controls, while the COA-STP1 spreading PREREG_ASSEMBLY_LAYOUT_2026-09-07 confirmed is exactly what it was. Pairs with Vrf:DropOriginVertexMeters (default 100), which stops a spread unit being marched back to the pile by its route's first vertex.",
    "DeStackCreates": true,
    "DeStackSpacingMeters": 700,

    "_DeStackComposedSiblings": "C14 AMENDED, USER RULING 2026-09-21 (option C of the D6 harvest, scratchpad validation/v6harvest/d6_harvest_report.md PART 2). The 2026-09-20 scope rule held a composed child on its parent's coordinate on the grounds that the PARENT'S FORMATION would lay it out (UG52 25.2.1). D6 measured what that actually produces on R9 lean: the three MechPlt aggregates 24-56 m apart at EVERY sample from creation to the destination, 3 of 3 platoon footprints overlapping, 137 member pairs under 7 m against D3's 44 - the vendor's parent formation does NOT separate composed sub-aggregates, so 'held with the parent' is 'stacked on the parent' and the 2026-09-07 criterion ('no two units' default formations can overlap whatever their heading') is violated 3 pairs out of 3. So siblings that share a coordinate - including one they hold only through the InitParser superior cascade - are spread, AROUND THE PARENT, WHICH DOES NOT MOVE (it keeps the centre slot, so a taskee's route still starts where the init put it), at THEIR OWN ECHELON'S spacing: platoon 350 m, section 300, squad 150, team 100 (EchelonSpacing.cs, derived from the shipped .frm formations with their leader chains resolved - the longest platoon formation, Formation-Column-US-Army-Mech-Plt-w-IFV.frm, spans 320.9 m). NOT the 700 m company number: 700 m against a 1,097 m route is 700 > 0.5 x 1097, which is exactly what put a whole platoon permanently under the STP-837 traversal bar in run D3. Company and above are deliberately NOT in the table and keep the ruled DeStackSpacingMeters. TURN IT OFF (false) for a run that must be comparable member-for-member with D1-D6: that restores the 2026-09-20 behaviour exactly (RUNBOOK 11e).",
    "DeStackComposedSiblings": true,

    "TimedCompletion": true,
    "DurationScale": 1.0,
    "TaskClock": "sim",
    "TaskPredecessorTimeoutSeconds": 600,
    "TaskPredecessorEndMarginSeconds": 60,
    "TaskChainBackstopSeconds": 86400,
    "SupersededTaskCode": "TASKABRT",

    "BackendLivenessSeconds": 10,
    "BackendLossConfirmSeconds": 30,
    "RequireNavAreaForGroundTasks": false,
    "NavAreaEvidenceSeconds": 300,

    "RouteExtentCheck": true,
    "MaxRouteLegKm": 50,
    "MaxVertexFromTaskeeKm": 100,

    "_PreflightRouteShift": "USER RULING 2026-09-20: 'Route shift: ON. Use as default for any run.' Written here as well as in VrfSettings.cs because a default that changes WHERE UNITS DRIVE must not live in C# only. Set false to turn it off (or Vrf__PreflightRouteShift=false in the app's environment; scripts\\StartInterface52.ps1 -RouteShift off). It never refuses a task: on a timeout, a throw, an empty tile cache or no cleared line, the AUTHORED line is dispatched. RUNBOOK sec 12.",
    "PreflightRouteShift": true,

    "_PreflightCacheDir": "F3 (cold-start review of f26d4ad). WHERE THE STREAMED TERRAIN TILES LIVE. EMPTY = the shipped fallback, '<app directory>\\preflight-cache' - which is INSIDE THE BUILD OUTPUT and is therefore WIPED BY A CLEAN REBUILD, so the first ground move of the next run re-fetches every tile over HTTP (60 s client timeout) against a 30 s route-shift deadline, gets no shift, and dispatches the authored line. Today's behaviour is kept as the fallback ON PURPOSE: a durable location outside the repo is not something this build creates on its own initiative. TO MAKE IT DURABLE, point this (or Vrf__PreflightCacheDir) at a directory that survives a rebuild - the interface creates it if it is missing, the file naming is byte-for-byte tools\\preflight\\leg_check.py's, and a warm copy of tools\\preflight\\preflight_cache can simply be dropped in. The resolved path and its tile count are on the start-up banner (ROUTE PRE-FLIGHT TILE CACHE), so a run can answer 'was it warm?' from its own log.",
    "PreflightCacheDir": "",

    "_PreflightOffline": "Never fetch a tile; score only what the cache already holds. A leg whose tiles are missing gets NO VERDICT - never a quiet pass. Written down here rather than left to the C# initialiser because it decides whether a dispatch can block on the network: with the cache warm, offline=true is the demo posture (no HTTP on any dispatch path); with the cache EMPTY, offline=true makes the route shift impossible and it is SKIPPED for the whole run, loudly.",
    "PreflightOffline": false,

    "_PreflightElevationLevel": "STP-802. The TMS level elevation dataset 149 is sampled at, and the FLOOR of its automatic per-tile fallback. Written here as well as in appsettings.Demo.json (added 2026-09-20): a runner-launched app loads THIS file only, and an AO-dependent value that lives in the C# initialiser alone is the asymmetry the route-shift merge's own principle forbids. 13 is the deepest level served over the MOJAVE AO and is what the 0.92 threshold was calibrated on; the Suwalki Gap is served at 12. Leaving the start at 13 with the floor at 11 lets one build serve either AO, and the interface logs which level each area resolved at.",
    "PreflightElevationLevel": 13,
    "PreflightElevationMinLevel": 11,

    "ArrivalMinTravelMeters": 100,

    "_ArrivalApproachFraction": "STP-837 AMENDED, USER RULING 2026-09-21 (option A of the D6 harvest). The traversal bar was max(0.5 x the ROUTE, ArrivalMinTravelMeters) for every member, which a member that legitimately starts closer than that to the destination can never show however completely it arrives - in run D3 twenty of 48 MechCoy members sat INSIDE the arrival radius and BELOW the bar at every sampled instant of the run, and at the moment run D6 closed the same task 42 of 57 D3 members were physically at the destination while the rule could count 23 (the whole 80 s gap). Each member is now asked for max(min(0.5 x route, THIS FRACTION x its OWN straight-line distance to the last vertex at dispatch), ArrivalMinTravelMeters) - never more than the old bar, so no task that closes today stops closing. 0.5 is chosen, not tuned: a member that starts d0 out and finishes inside the effective radius R shows at least d0 - R of displacement, and d0 - R >= 0.5 d0 exactly when d0 >= 2R, so 'actually drove there' satisfies it for every member starting two radii out - the property the route-level 0.5 had. V6g is NOT re-opened: a route whose last vertex lies inside the arrival radius of the dispatch position is refused by ClosableByArrival before any member is sampled, and that gate is untouched (--arrival-selftest replays the real V6g geometry and shows the refusal). Set to 0 to restore the pure route-length bar for a run that must reproduce a pre-2026-09-21 result.",
    "ArrivalApproachFraction": 0.5
  }
}
