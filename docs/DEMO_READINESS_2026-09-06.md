# DEMO-READY: the interface as a standalone deployment (no harness)

User ruling 2026-09-06 (RL-20260906-01, archive): "When the time comes for tackling the real tasks, not just move along,
let's first get the app ready for deployment as a demo, working without the harness for this
bit." SEQUENCE: (1) finish the current move-along / scale measurement (COA-STP1 on 5.2 - since
2026-09-06 22:00Z: 11 taskees with order-time materialization, C13), then (2) THIS milestone,
then (3) ADDED 2026-09-06 ("Add to the plan as suggested"): the aggregate-level profile, Y-15
realized - docs/PLAN_AGGREGATE_LEVEL_PROFILE_2026-09-06.md, then (4) the STP task vocabulary
(goal-full-stp-task-vocabulary) targeting both model sets.

"Without the harness" = the operator starts VR-Forces and the interface by hand (or by ONE thin
start script that only sets the interface's own environment), STP pushes the init and orders to
the C2SIM server, the interface creates the ORBAT and executes the orders, and STP / the
VR-Forces GUI show the result. No runner, no WatchVrf, no ledger, no scoring - those stay the
TEST harness. The GUI is the audience's window (a human watching), which is not GUI automation
and does not contradict the headless goal; the interface itself stays headless.

## Gap list (D = done and verified today or earlier; O = owed; V = exists, needs one verification)

| # | Requirement | Status | Evidence / what is owed |
|---|---|---|---|
| 1 | ORBAT loads faithfully (declared tree composed; coarse leaves expanded; leader = declared first) | D | N1/N2/N3 2026-09-06: PREREG_N1/N2/N3; DESIGN_ORBAT C1-C11 |
| 2 | Units MOVE as units on 5.2 (company) | D | compose 3/3 x5 runs; C1b closed by the console. AT SCALE 2026-09-07 (PREREG_ASSEMBLY_LAYOUT): the 11-unit COA, spread on 700 m rings (C14), drives 24-33 km legs and reports the first two TASKCMPLT at scale; demo overlay carries CreationPolicy=AtOrder + DeStackCreates/700 + TaskPredecessorTimeoutSeconds 7200. The 9-hour "no completion" question is CLOSED by C15 (arrival-evidence completion, verified P9). FOUND 2026-09-07 (3f): STP's routes start at the unit's own position, so spread units marched back into the assembly point and jammed there - Vrf:DropOriginVertexMeters (default 100) drops that leading vertex; verification run P11. **D6 2026-09-20:** commit 2746a0d (merged 248143f) narrowed de-stack to exempt composed children (DeStack (C14 scope)) - on R9 lean this stacks three sibling platoons into one 126 m blob (was three disjoint 700 m rings), which VIOLATES the 2026-09-07 ruling's own geometric criterion (700 m > 630 m company span) though no jam is observed at R9 scale (object consoles were off, so the ruling's own instrument could not score it). OPEN, not decided: option A (fix the STP-837 traversal bar to use each member's own distance-to-destination), option C (spread composed siblings at the echelon's own ~300 m scale instead of the company's 700 m), or option D (keep 2746a0d as merged). Seat recommends A+C. **2026-09-21:** user ruling A + C (RL-20260921-01) merged adca180 (platoon siblings 350 m; per-member arrival bar); confirmation run D7 registered. **OPEN, flagged 2026-09-21 (U2 lane D):** this row records 350 m as his ruling, but the recommendation he approved with "As recommended" said about 300 m for platoons (RL-20260921-01 carries both). The number in the code is NOT changed here; either the number or the attribution is wrong and it is his to settle. **2026-09-21 symmetric ring merged 1d0fb69 (parent's route origin restored); D8 registered.** |
| 3 | Task-status (TASKCMPLT) reports to the C2SIM bus | D | every run since R9; AT SCALE 2026-09-07: two vendor completions per spaced run (003457Z, 103246Z) after 24-29 km legs; the interface's own arrival-evidence completion (C15) covers units the vendor holds behind a straggler - VERIFIED run 114026Z (P9): 4 evidence completions incl. 1-35 with a stuck M3 (4 of 6 within 500 m), 3 later vendor completions swallowed, one TASKCMPLT per task, no false report |
| 4 | POSITION reports to the C2SIM bus on 5.2 | D | R1 (PREREG_R1) and AT SCALE 2026-09-07: run 103246Z captured 69,314 PositionReports over 45 min (127 units every 10 s); appsettings.Demo.json PositionReportSeconds 10 |
| 5 | appsettings.json DEMO defaults | D | `src/VrfC2SimApp/appsettings.Demo.json` (2026-09-06): the 5.2 profile keys the runner injects (ConfigFileIdentity, Federation "", FedFileName "", ConnectionConfigFile MAK-ONE-2025, TypeMapFile 52, VrfHome) + ClientId STP (override per init), compose ON, PositionReportSeconds 10, console levels -1. Loaded by DOTNET_ENVIRONMENT=Demo (HostApplicationBuilder default overlay); csproj copies it. Owed: one standalone start with it (row 7's run). **2026-09-20 D1/D1b/D2:** three GUI-on one-command runs used this profile (DOTNET_ENVIRONMENT=Demo) end to end; the owed standalone start is done. **2026-09-21 CORRECTION/D5b:** the D1/D1b/D2 runs above went through `scripts/RunScenario.sh` (Way A, the one-command wrapper), not the hand-started standalone sequence (Way B: StartRtiExec52 -> LaunchVrf52 -> StartInterface52) - the two are different startup paths and this row's "standalone start is done" overstated it. D5/D5b are the actual Way B rehearsals: D5b's steps 1-3 (rtiexec, LaunchVrf52 with its own holder, StartInterface52) ran end to end and HIT with this same profile; row stays V for the profile itself, the standalone-PATH claim is corrected in row 7. **2026-09-21 D5c:** the Way B confirming run (binary 4f1f149) completed 3/3 TASKCMPLT with this same profile end to end - row now D, verified. **2026-09-21 D5d:** re-confirmed on the route-origin build 51d59c0 (STP-855) - 3/3 TASKCMPLT, this same profile, no regression. |
| 6 | Runtime environment without the runner | V | IN-PROCESS since 2026-09-07 (user: "I need C# code I can send to the solutions engineers"): `src/VrfC2SimApp/MakRuntime.cs` reads Vrf:VrfHome/VrLinkHome/RtiHome/RidFile/RtiAssistantDisable from appsettings and prepares the process itself (PATH prefix, MAK_*DIR, RTI_RID_FILE, RTI_ASSISTANT_DISABLE) before the bridge loads; the rid ships beside the exe; a configured value overrides a differing process variable (logged; a stale machine-level MAK_VRLDIR=5.8 on the dev box was the case that decided it), empty keys leave the environment alone. The deliverable = the published folder + appsettings; `scripts/StartInterface52.ps1` is now optional convenience (it still selects DOTNET_ENVIRONMENT=Demo and the per-run overrides). The product contains no Python; Python is test-harness analysis only. `VrfC2SimApp --runtime-check` is the deployment smoke test (applies the bootstrap, binds the bridge, prints the stack, exits; negative control with wrong roots FAILS loudly - verified 2026-09-07). FOUND BY IT: the host resolved appsettings against the CURRENT DIRECTORY, so an exe started from any other folder ran without its settings (the runner and the script set the cwd, which hid it) - FIXED: ContentRootPath = the exe's folder. Owed: one standalone start on the deployed build. **2026-09-20 D1/D1b/D2:** three runs started the interface via the deployed build under the runner wrapper and joined/reported cleanly each time; the standalone `--runtime-check` smoke test itself was not separately re-exercised on this build. |
| 7 | VR-Forces GUI ON + standalone start | D | every 5.2 run today was -NoGui and runner-started. ONE end-to-end standalone run owed: StartRtiExec52 -> LaunchVrf52 (GUI) -> StartInterface52 -> PushInit/PushOrder as the STP stand-in; the interface path is expected unchanged (UG52 4.1.2 independent mode; the GUI is a second federate). **2026-09-20 D1/D1b/D2:** the exact sequence ran end to end three times via the one-command wrapper (GUI on, RTI holder, interface, PushInit, PushOrder), all three reaching READY and completing 3/3. **2026-09-21 CORRECTION/D5-D5b:** the D1/D1b/D2 runs above are Way A (`scripts/RunScenario.sh --gui`, one process tree, one script); they are NOT the hand-started standalone sequence this row asks for. That sequence is Way B, and its first live rehearsal (D5) found the standalone holder block in `LaunchVrf52.ps1` dead at step 2 since 2026-09-20 (undefined `Say-Info`, fixed 1553e48); the re-run (D5b) got PAST that for the first time - steps 1-3 (rtiexec, LaunchVrf52's own holder + back end + GUI, StartInterface52) all HIT - but the back end then crashed 1.3 s after the order arrived (STP-854, undiagnosed) and 0 of 3 tasks completed. Row reopened to O: Way B has now shown its start-up steps work, not a completed run. Blocked on STP-852 (defer-dispatch-until-init-bound / READY TO TASK) being merged, rebuilt and deployed, then a clean confirming run (C2). **2026-09-21 D5c:** the confirming run on binary 4f1f149 (STP-852 merged) - GUI on, LaunchVrf52's own standalone holder, StartInterface52, hand-started end to end via PushInit/PushOrder standing in for STP - reached READY TO TASK, held two early tasks 0.56 s/0.10 s and released them correctly, and completed 3/3 TASKCMPLT with a clean teardown; no back-end fault (STP-854 not reproduced here). Row now D: Way B verified end to end with PushInit/PushOrder standing in for STP, on the private 18080/61614 server, on the R9 Mojave fixture. Still owed and explicitly open, not closed by this run: STP itself pushing, a human at the keyboard, the standard 8080/61613 server, and Iron Storm (row 25/HANDOFF); STP-853 untested here; STP-855 (the composed-aggregate route-origin race, D9) was avoided by timing, not fixed. **2026-09-21 D5d:** re-run on the route-origin build 51d59c0 - every limb HIT, 3/3 TASKCMPLT, and this time the route-origin fix (STP-855) is CONFIRMED, not merely avoided (see row 10). |
| 8 | ApplicationNumber without the ledger | D | demo block 9101-9199 in appsettings.Demo.json (outside the test ledger's 4xxx range); `-AppNumber` on the start script; "two interfaces on one network must differ" documented in the overlay and the runbook |
| 9 | Reset between demo runs | D | tools/ResetVrf (RUNBOOK sec 8) exists; verify a clean "reload scenario -> push init again" cycle without restarting VR-Forces (duplicate-init guard exists: VrfC2SimService.cs:577). **2026-09-20 D1:** the full-cycle reset with the GUI is blocked by STP-844 - a leftover vrfGui (stuck on its exit-prompt stack) blocks the next LaunchVrf until closed by hand. **2026-09-20 D2:** functional reset VERIFIED for the FULL CYCLE (one command, no wedge, no blind observers, fresh holder appNo); second cycle ran at 0.60x entity speed, cause open - leave a few minutes between cycles (discriminator = D3). |
| 10 | Terrain + real-time fixture | O | MAK Earth (online) needs internet; an offline fallback terrain (a shipped SharedData terrain) should be named in the demo runbook. Real-time fixture BUILT 2026-09-06: `tools/FixtureGen/frame_variants/R9_Mojave_Empty_52_RT.scnx` (build_fixture.py --profile 5.2 --empty --frame-mode variable-frame --out-name R9_Mojave_Empty_52_RT; frame-time 0.1; validate_fixture.py passes every structural check and fails only its two FFRTC-pair assertions, frame-mode/frame-time, by design). Owed: the sanctioned deploy to C:\MAK\vrforces5.2d\userData\scenarios (same command + --out-dir) and one run. **2026-09-20 D1:** the R9_Mojave_Empty_52 GUI-on one-command run (not this FixtureGen RT fixture) ran in real time, ratio 1.00, 4 min 44 s order-to-last-completion; this readiness item may already be satisfied by that scenario - confirm before closing the row (d1_harvest_report.md sec 5). **2026-09-21 CORRECTION:** the D1 "real time, ratio 1.00" reading above is WRONG - D7's harvest measured D1-D7's run-to-complete frame mode at ~3.00x the wall clock, unit inferred (see docs/experiments/PREREG_DEMO_REHEARSAL_2026-09-20.md D7 RESULT N7); R9_Mojave_Empty_52 most likely ran at ~3x, not real time; the variable-frame RT fixture built above exists but has NEVER been deployed or rehearsed; status reopened to O pending an actual real-time (variable-frame) run. **D8 2026-09-21 (RESULT N7/N11):** the unit is now SETTLED (the app's own sim clock, measured directly, agrees with the console clock to 0.4 s) but "3.00x" is REFINED to "the loaded-movement rate" only - within one run the ratio is LOAD-DEPENDENT, ranging 2.63x (peak load) to ~4.7x (idle); R9's numbers are not a prediction for another scenario (COA-STP1 at scale once measured 0.27x, slower than real time). **OPEN DECISION (the user's):** demo clock - accept the load-dependent fast clock as-is (state it to the audience) vs deploy and rehearse the never-used real-time (variable-frame) fixture built above. **2026-09-21 RULED (RL-20260921-02 item 3, his words "3. Fast."):** the user closed the open decision - ACCEPT the load-dependent run-to-complete clock (2.6x-4.7x measured on R9, load-dependent, never "real time"); the real-time (variable-frame) fixture built 2026-09-06 is NOT needed and will not be deployed. Real-time-fixture part of this row: CLOSED by ruling, no further work. Terrain part of this row (MAK Earth offline fallback naming) remains separately OWED - row stays O for that reason alone. N13/STP-855 (D9) - a composed aggregate's route origin can be read inside VR-Forces' own re-compose transient, up to R/2 off the authored coordinate; this is a HIGH-limb miss that REOPENS build 4f1f149 and blocks any demo-ready claim for it until fixed, reviewed, rebuilt and confirmed. STP-853 (dead-sim-reported-healthy) and STP-854 (D5b back-end crash, n=1) stay OPEN, unresolved by D9/D5c. MAK support case: OPEN (RL-20260921-02 item 5, his words "5. Open.") - the seat prepares the package and a draft; the user sends it (package under scratch validation\mak_case\; detail in RUNBOOK sec 9c). **2026-09-21 D10/D5d: N13/STP-855 FIXED AND CONFIRMED LIVE on binary 51d59c0.** D10 (Way A) landed inside the exact re-compose transient that produced the original defect and the route still came out right (origin 0.04 m off authored); D5d (Way B) exercised the other provenance branch (the name-registry read of the deleted shells) and got the same result. Both orderings on one binary now produce the correct route. STP-855 CLOSED. |
| 10a | Cross-country traversability (2026-09-07, PREREG_ASSEMBLY_LAYOUT 3e) | O (user) | Units stall silently on the cross-country approach from their slot and on fixed spots of the routes (a grade above the vehicle's max-slope; the vendor's obstacle planner ignores slope, UG52 23.5, and military vehicles ignore roads by default, 40.54). The vendor's traversability-aware planner is NAVIGATION DATA (UG52 ch 66: a NavMesh per navigation profile - slope-max, step-max, entity radius - generated from the GUI for a navigation area; "included with VR-Forces terrain databases", generating it "requires an additional license", 5.x). MAK Earth (online) has none for the Mojave AOI. Options: (a) generate navigation data for the demo AOI - FEASIBLE HEADLESS, verified 2026-09-07 17:00Z: bin64\vrfNavGenerator.exe (the process the GUI spawns) generated a 3x3 km ground-platform area in 47 s under the DEMO licence (PREREG_ASSEMBLY_LAYOUT 3g RESULTS; traps: MAX_PATH, --outputPath is emptied first, --navDataDir must be given); loading = a navData record in a COPY of the terrain .mtf (UG52 66.5); 20x20 km per area, several areas per terrain (66.2) - the COA's 39x52 km route box needs ~6 areas; live gate = the probe run on a 20 km area; (b) Prefer Roads for the tasked units (DtSetNavigationPreference set-data, UG52 40.54); (c) the aggregate-level profile, which has no vehicle dynamics. MEASURED 2026-09-13 (PREREG_NAVDATA_G3, three runs compared at EQUAL simulated time): navigation data over the assembly ground gives the COA NO speed effect (0.98x of the no-mesh run at sim 1170 and 2500), slightly tighter formations, and does NOT free the early stops - 1-35/2/1_A froze at 1.97 km in all three runs with the slope-aware planner active over it, 1-6/2/1_AD stopped in three different ways. The slope-blind-planner explanation of those stops is REFUTED for 1-35. The G2 "engine collapse" was a concurrent agent load on the machine, not the mesh (G3: steady 1.6x, ~5 cores). What stops those units is the open question. CONSOLES READ 2026-09-13 (FINDING_EARLY_STOPS_2026-09-13): the sim reports the move as running / unblocked / goal unchanged forever, enters no failure branch, and the units freeze on near-level ground just past a crest after climbing 41-42 deg pitches; the slope explanation is REFUTED as stated (grade at the stop -1 to +6 %; a same-type unit crossed the ridge 400 m away); G5 (1-35 alone, console debug level 4) RAN: the freeze is intrinsic (same point to 1 m alone), and the console prints commands, never the vehicle's response. VENDOR PASS (FINDING sec 6): a vehicle that stops while its task runs is UNDETECTED BY DESIGN in VR-Forces 5.2 (base give-up test "always returns false"; the decideToGiveUpTask SAMPLE shows the integrator adding the test); soil under the stop read from the streamed tiles = sand (0.80), not water/muck; CAUSE (FINDING sec 7; cold-start reviewed 2026-09-13, STANDS WITH FIXES): the sim's surface here is the streamed 10 m California inset (8 m posting, validated to 3 cm against the vehicles' reported altitudes); ahead of 1-35's freeze seven consecutive postings rise at 0.70-0.95 over 55 m on sand (mean 0.858) - a SUSTAINED face no leader in any run surmounted (max 55 m window surmounted 0.714; the control line over the same ridge 0.19-0.22, 55 m max 0.661); the leader sits at the toe, where a limit near 0.75 (not the raw 0.94) bites; a HMMWV with a higher limit stops 61 m further up the same face. STP's straight legs cross ridge faces the tanks cannot climb, our authoring sampled only the four vertices, and the vendor tests no progress. Not excluded: the formation's mutual speed control pinning the leader - the confirming test is a run that releases 1-35 by a route around the face or by Prefer Roads. DEMO IMPLICATION regardless of the final cause (CORRECTION 2026-09-21: this said "user decision owed"; it was not owed - he gave it on 2026-09-13, RL-20260913-03, archive): the interface needs its OWN progress watchdog - no net movement for N sim-seconds while the task reports running -> report to STP and/or re-task - the sibling of the arrival-evidence rule (C15); and route authoring should test max-slope x soil factor along the leg, not vertex heights alone (row 18's validator extended to the leg). Decision owed (user): whether option (a) stays in the demo at all given these numbers; where the terrain copy + area live if it does (vendor default userData\navData, under C:\MAK) |
| 10b | Type mapping = the fidelity table | D | appsettings.Demo.json sets TypeMappingMode=FidelityTable + the 5.2 map. Found on COA-STP1 run 1 (2026-09-06): the base default RealTemplates is the 5.0.2 parity dispatch (every company = Tank Company (USA), hostile included); the table is read only in FidelityTable mode. Its live gate = COA-STP1 run 2 |
| 11 | Type-map coverage of the DEMO ORBAT | V | The type-map LIVE gate passed on the small fixture 2026-09-06 (run L3 201623Z: FidelityTable mode, ~PXY proxies surfaced, 3/3 completion incl. a composed tank company and a BN CP proxy); at scale S4 created all 128 (scratch no-lifeform map). For the demo's real init: `--parse-init <init> <clientId>` + `--typemap-selftest`; lifeform rows need row 17 first; PROXY rows (BN -> HQ-section CP) are a fidelity decision to state in the demo notes. **2026-09-20 D1/D1b/D2:** the same lean-init/fidelity-table fixture ran three more times (0 STP-833 refusals each) - consistent with, not additional evidence beyond, the L3 live gate already recorded here. |
| 12 | Licence | D until 2026-10-31 | RENEWED 2026-09-14 (node-locked DEMO, whole toolchain; User-scope MAKLMGRD_LICENSE_FILE). Lapses 2026-10-31 - renew via MAK Sales before any demo after that date (user) |
| 13 | Demo runbook | O | docs/DEMO_RUNBOOK.md: prerequisites, start order (rtiexec -> VR-Forces with GUI -> interface -> STP init -> orders), what the audience sees, the three traps (PATH, RTI dialog once per reboot, leftover instances), stop order DRAFTED 2026-09-14 ~13:20Z: docs/DEMO_RUNBOOK.md rewritten for the operator (Way A one command via scripts/RunScenario.sh, Way B STP-driven; READY line; push endpoints; reset; clean stop; three common failures; UNVERIFIED items listed - the --gui wrapper path and the STP-driven sequence end to end are unrehearsed). **2026-09-20 D1:** the --gui one-command run (scripts/RunScenario.sh --gui) reached READY unattended and completed 3/3 in 4 min 44 s order-to-last-completion (watchable); teardown left the GUI open on its exit prompt (row 14). **2026-09-21 CORRECTION:** "in real time (ratio 1.00)" above is WITHDRAWN - D7/D8 measured this run-to-complete frame mode at a load-dependent 2.6-4.7x, not 1.00x (see row 10; docs/experiments/PREREG_DEMO_REHEARSAL_2026-09-20.md D7 RESULT N7 / D8 RESULT N11); the 4 min 44 s wall figure stands, it is simply not real time. **2026-09-20 D1/D1b/D2:** Way A rehearsed end to end with the GUI three times (D1, D1b, D2); Way B (STP-driven, hand-started) is still unrehearsed. **2026-09-21 D5/D5b:** Way B is no longer unrehearsed - steps 1-3 as typed in this runbook now HIT live (D5 found and D5's own fix line closed a dead LaunchVrf52 branch; D5b then ran rtiexec -> LaunchVrf52 -> StartInterface52 end to end) - but the back end crashed 1.3 s after the order was pushed and 0 tasks completed (STP-854), and a second finding (STP-853) is demo-relevant on its own: the interface reported the dead simulation as healthy for 158 s and dispatched a task into it. This runbook still has no line telling an operator when it is safe to push the order after the init (STP-852); row 13 stays O pending that fix and a clean end-to-end Way B run (C2). **2026-09-21 D5c:** STP-852 merged and the clean confirming run (C2) landed - Way B's HALF of this row is now VERIFIED (steps 1-4 typed in the runbook, plus the stop order, ran end to end with 3/3 TASKCMPLT; DEMO_RUNBOOK.md sec 2's "UNVERIFIED as a single sequence" note is corrected in the same pass). Row stays O overall: still owed - STP itself pushing (not PushInit/PushOrder standing in), a human at the keyboard reading the READY/READY-TO-TASK/server lines, the standard 8080/61613 server, and Iron Storm (the demo scenario, not yet rehearsed on either Way). **2026-09-21 D5d:** re-confirmed on the route-origin build 51d59c0 - every limb HIT, no regression; Iron Storm itself was diagnostically driven the same day (row 16) but is separately blocked by STP-856/STP-857, not by anything in this row's Way B sequence. |
| 14 | Clean stop | D | the interface's Stop path resigns (VrfC2SimService.StopAsync); VR-Forces stays up; the harness's teardown problems (memory: "TEARDOWN IS NOT SOLVED") are harness problems - verify the interface alone stops cleanly and can be restarted against the running sim. **2026-09-20 D1:** clean stop with the GUI is OPEN - vrfGui pid 39652 survived StopVrf52.ps1 (exit 3) on its documented exit prompt (UG52 4.6/4.6.1, myShowQuitDialogOnClose=1); FIRST 5.2 GUI-on teardown ever (1 of 80 StopVrf52 runs), the headless 8/8 and 79/79 clean record is unaffected; remedy lane fix/gui-quit-prompt-teardown. GUI-on clean stop OPEN - STP-844; headless clean stop unchanged (79/79). Remedy merged 8972832 (run-owned appData, both prompts off, no GUI automation); confirming run D1b registered. **D1b 2026-09-20:** GUI-on teardown clean in 9.9 s (STP-844 fixed, run-owned appData). |
| 15 | Observability for the operator | O | the app log at INFO is the operator's view (run P: endpoints, bridge start, "Backend discovered", "Connected to C2SIM ... clientId", "C2SIM Initialization", mode line, compose lines, "Init dispatched: N units", PLACEMENT per unit, then per-task CreateRoute/MoveAlongRoute and "VRF task complete"). Owed: one explicit "READY - joined, N backend(s), type map <mode>, waiting for the initialization" line after the join, and a per-order summary ("order <id>: N tasks for M taskees"); the console channel stays -1 REPORTING ASSESSMENT 2026-09-14 (REPORTING_ASSESSMENT_2026-09-14.md sec 5 G9): the READY line and the per-order summary are DONE in the shipped build (G6 log); still missing on the reporting side - the R1 skip counter never names the unit and conflates two failures (hid gap G1 for five runs), no cumulative sent/failed counter (129 silent losses in G6), the loudest log line is a harmless SDK deserialize error (2 per run, B5). B2/B3/B5 in flight on feat/reporting. BUILT 2026-09-14 ~13:00Z on feat/reporting (B5 9286398, B3 ebf8da9+f0d1c68, B2 f057fc2, plus the cold-start review's fixes), NOT YET CONFIRMED LIVE: the R1 line now names the unit the first time it is skipped and separates NAME UNRESOLVED from NOT REFLECTED, the cumulative delivery pair is labelled "cumulative: N sent, M failed", a failed push is LOUD and a task status is retried, and the speculative deserialize is gone. The row stays O until one run shows 128 sent / 0 skipped and zero "Failed to deserialize" lines. **D6 2026-09-20:** runner truth 7/7 live (input sources with provenance, route shift predicted vs announced CONFIRMED, vendor logs by pid, config hash) - the runner now tells the operator what it actually launched and can be trusted where it agrees with the app. |
| 16 | Real tasks | later | **NOW:** V2/V13 LIVE (23:09Z/00:15Z) dispatched STP's real task vocabulary on the bus off the merged rulings build (Q4 refusal, TASKSTRT once per task, 40 timed TASKCMPLT) - next: an STP-side fix for STP-800 (TaskStatus/Observations discarded on receipt), then the ATTACK/HoldObjective families (V6 on). after this milestone: the 17 verbs of the STP vocabulary (VerbMapping: recognized, Layer-2 not wired). Seen 2026-09-07 (run 103246Z): the ADA battery's DEFEND task ("provide air defence coverage", no location) maps to HoldObjective and is refused with "NO LOCATION GIVEN - CAN'T EXECUTE", which abandons its three successors; a coverage verb with no location needs a non-move intent (hold in place / automatic air defence, UG52 35.2.9). MOVEMENT INSIDE THE REAL TASKS IS THE VENDOR'S (2026-09-07, user: "How is this going to work when we get to real tasks?"): Movement to Contact / Seize Objective / Attack by Fire etc. are vendor unit tasks whose controllers move the unit (UG52 33.7.6 p677, 33.7.7 p678); the interface maps a verb to ONE vendor task and never re-plans routes itself (the per-vertex task split was withdrawn, PREREG_ASSEMBLY_LAYOUT 3g). Terrain-aware movement comes from navigation data, which acts on every planned leg regardless of the task (UG52 30.28 p604; Migration Guide 2.4 p18) - row 10a LANE OPENED 2026-09-14 12:50Z (plan L8): docs-first vocabulary assessment running; Jira epic in STP. ASSESSMENT DONE 13:15Z (TASK_VOCABULARY_ASSESSMENT_2026-09-14.md): the vendor's scripted-task channel is wired in the facade with zero callers; 5.2 tactical tasks are all Lua scripts (many shipped in EntityLevel: company_seize, co_clear, company_breach, plt_attack_by_fire, ...); the init's 35 objective areas already exist in VR-Forces by C2SIM uuid; the constraint is PARAMETER SUPPLY (9/42 tasks have no location; 42/42 self-target); build list V1-V11; six user rulings R1-R6 owed (R1-R4 RL-20260914-02; R5 RL-20260914-03; R6 RL-20260914-04, both archive). USER RULINGS Q1-Q7 IN 2026-09-14 20:00Z (RL-20260914-05, archive) (docs-first pass complete): Q1 a superseded task -> TASKABRT and its successors are abandoned; Q2 Duration is measured on the sim clock; Q3 wire ambiguity accepted; Q4 no Duration and no geometry = MALFORMED, refused with an error plus TASKABRT (no invented hold); Q5 a paused scenario holds task time while the back end reports; Q6 no interim stop-gap; Q7 rollback is forward-only. Fix pass 2 LANDED on feat/tasking-rulings @ 8db033e (A1 chain gate: COA-STP1 42 dispatches / 0 skips offline, deterministic; A2, B1, B4, B6, B7; Q4 and Q5 built; 18 suites, rulings suite 112 checks); pass-3 cold-start review RUNNING before merge; follow-up STP-809 (the facade must expose back-end status Paused/Playing/gone so a dead back end does not freeze task time under Q5). V2 LIVE 23:09Z (230706Z, merged build): the 42-task STREND chain dispatched 41/42 (one vendor task failure -> TASKABRT -> its successor skipped, as ruled), 40 timed TASKCMPLT at Duration x 0.05 of SIM time, T13 after its scaled delay, 9 in-place, 0 malformed, 0 push failures - the rulings build works on the bus; the only defect was the runner's stop rule (fix dispatched). V13 00:15Z: Q4 malformed refusal LIVE (TASKABRT at receipt, no TASKSTRT), TASKSTRT once per task, one code per outcome, 0 push loss - the reporting gates PASS on the bus capture (STP still discards TaskStatus: STP-800). **Iron Storm cut A, 2026-09-21:** cut A derived files + T14 nudge ready (branch feat/ironstorm-cut-a 438a9e4, not merged); nav gate at default precision passes ONLY T14 (T02 3 of 16 sectors < 0.9 worst 0.880, T10 4 of 8 worst 0.818; nothing below 0.5); re-tiling futile, precision 0.4 meshes over lakes (fidelity FAIL); the 0.5-0.9 band has never been driven - decision with the user: a pre-registered diagnostic drive vs a 1-leg cut. **2026-09-21 DIAGNOSTIC DRIVE RESULT (user-authorised, binary 51d59c0):** the fixture (IronStorm_Centre_52_Nav_AG, deployed under C:\MAK, sha256 d379dd68...aa4bb) IS deployed and drivable - nav data loaded, back end healthy throughout, tiles fetched clean. T10 (1-112 IN) crossed all four of its sub-0.8182-0.8868 sectors end to end without freezing and arrived within 2.9 m, +1.9% path - the first drive in the record between 0.5 and 1.00 connectivity, though it crawled at 0.28 m/s (2x the stall threshold), cause not adjudicated. But T02 and T14 (the control) never moved a single metre - both were created off the terrain at FALLBACK altitude 0 after the init's one terrain-profile query timed out (36 of 36 creates), measuring -0.0 m against 145.4 m / 155.8 m of terrain on the independent WatchVrf trace for the whole 40-minute run. **2026-09-21 CORRECTION: WHY they did not move is NOT SHOWN** - docs/VRF_ALTITUDE_FRAMES.md sec 5 falsifies "born buried, therefore never moves"; an earlier version of this row stated burial as the settled cause and that causal claim is WITHDRAWN (see docs/experiments/PREREG_IRONSTORM_DRIVE_2026-09-21.md's CORRECTION blocks). **STP-856**, correctly scoped, is the fallback-altitude CREATE DEFECT itself (a hole in the 2026-09-05 birth-altitude cure, not a freeze diagnosis), and it still BLOCKS both full cut A and its registered T14-only fallback (48 IBCT is one of the two affected platforms) - neither is demonstrable today regardless of what actually stops them moving. **STP-857** (a MOVE task timed-completes though the unit never moved, and the C16 stall TASKABRT is suppressed by the already-fired completion) is open and needs a completion-semantics ruling. The 0.82-0.90 band question remains OPEN; the 0.9 bar is not reopened by this run. NEXT: one re-run after STP-856 is fixed (pre-warm terrain or hold on READY TO TASK), scoring "PLACEMENT summary: 36 of 36 ... TERRAIN QUERY" and "READY TO TASK - 36 of 36 bound". |
| 18 | Slot validator for the assembly-area layout (C14) | O | ADDED 2026-09-07 (user: "since we are doing the scattering, placing units on unfit terrain is on us ... add building the slot validator to the list"). Before a ring slot is used: heights on a 50 m star around it from the terrain-profile request (already sent for placement) -> local grade vs the unit's vehicles' max-slope (entity parameter; UG52 23.5 "vehicles can just barely move up the max-slope"); soilType from the same reply (the facade drops it today - forward it) vs the water/muck classes of UG52 Table 26; a failing slot is skipped for the next ring slot, with the reason logged. Cannot see feature obstacles (no remote query exists); backstop = a unit whose members have not moved N s after tasking is re-slotted. Evidence: PREREG_ASSEMBLY_LAYOUT 3d/3e (stalls happen on the cross-country approach from the slot as much as under it - the validator should also test the first km toward the route's first vertex). Offline self-test + one run |
| 19 | Progress watchdog (stall detection) - APPROVED 2026-09-13 ("2 as recommended") | O | **NOW:** C16 LIVE PASS (V13, 00:15Z): fired (report-only TASKABRT) on the frozen 1-35 and stayed silent on the moving 1-1; merged to main via feat/integration (0f4d09e); enable via Vrf__StallDetection=true (still default OFF). The vendor never reports a unit that stops making progress: singleTaskControllerComponent.h:192-205 "always returns false", the move-to Lua has no progress test, the decideToGiveUpTask SAMPLE hands the test to the integrator (FINDING_EARLY_STOPS sec 6a). BUILD, report-only: a tasked unit whose members show no net movement for N sim-seconds (N = 120) while its task reports running -> a C2SIM TaskStatus report to STP with TASKABRT (the schema's codes: TASKPEND/TASKSTRT/TASKINPRG/TASKCMPLT/TASKABRT; no automatic re-task); the vehicle position/velocity we already reflect carries it; sibling of C15. Open with the user: whether TASKABRT is the code STP should see, or a distinct rendering on their side. Offline self-test + one run against the known 1-35 freeze (must fire by sim ~500) and the known-good units (must NOT fire) USER RULING 2026-09-14 (RL-20260914-01, archive; the "~12:40Z" stamp this row used is wrong - the record stamps it 11:15:53Z) ("Ok on 2-3"): TASKABRT IS the TaskStatus code STP sees for a stalled unit - decided; ABORT-THEN-COMPLETE (a later TASKCMPLT is never suppressed) is a SUPERVISOR position (2026-09-13; RL-20260914-01 covers the CODE only, see its scope note). REPORTING ASSESSMENT 2026-09-14: G6 emitted 0 TASKABRT and only TASKCMPLT in 9 h; the watchdog (default OFF) is the only path that would have reported the frozen units. Code decided TASKABRT (user). Merge after pass 4. BUILT on the C16 branch (report-only, Vrf:StallDetection default FALSE): StallPolicy.cs + MaybeCheckStalls + ReportBuilder.BuildTaskStatusReport(code) + --stall-selftest + --report-selftest TASKABRT round-trip + --arrival-selftest (incl. the shared-sampler de-duplication guard); design C16 in DESIGN_ORBAT_TO_VRF_2026-09-06.md. ABORT-THEN-COMPLETE is a SUPERVISOR position (2026-09-13; RL-20260914-01 covers the CODE only, see its scope note): a unit that reported TASKABRT and later ARRIVES still sends TASKCMPLT for the same task uuid; TASKABRT never suppresses a later TASKCMPLT, while a TASKCMPLT does suppress any later TASKABRT for that task. REPLAY-VALIDATED offline (tools/analysis/stall_replay.py) on 3 runs at window 240 s / 50 m, re-run 2026-09-13 after the cold-start review, invocations recorded in C16 and in the tool: G5 185936Z (6.21x) 1-35 fires at wall 385 s, 24,304 m short; G3 174516Z (1.60x) 1-35 at 431 s and 1-6 at 742 s, 24-30 km short, 7 true negatives; P11 150643Z (1.46x) 1-35 at 419 s and 1-6 at 1824 s, 7 true negatives; 0 false alarms, 0 misses. The window was RAISED from the row's original 120 to 240 because 120 false-alarmed on four crawling units in P11 (they covered 203-1,191 m after the abort would have fired); measured, the last false alarm dies between window 160 and 170, so 240 keeps a 1.41x margin, and at 240 any move threshold from 35 to 70 m is clean and still catches both frozen units. G3's 856/HHC stops 3,448 m short 135 s before its trace ends - too late for a 240 s window and UNDECIDABLE as a true positive (the same unit is a crawler in P11); see C16. CLOCK: the window's clock is SELECTABLE (Vrf:StallClock; default "wall" 240 s, "sim" 360 s - both calibrated, C16 and docs/experiments/RECAL_STALL_SIMSECONDS_2026-09-13.md). The sim reader (VrfBridge.SimTimeSeconds -> VrfFacade::SimTimeSeconds -> DtVrfRemoteController::simTime) LANDED with the C16 branch, so the earlier "the facade exports no sim-clock reader, OWED after G6" note is SUPERSEDED. Not deployed - deploy under a new prereg. STP-809 BUILT+MERGED 00:10Z (4cd84d7): the Q5 hold now reads the vendor's backendsControlState and an ACTIVE back-end count - a back end that dies in place falls to WALL instead of freezing task time; 148/0 suite. V5 (PREREG_V5, 0e9884e) ran live gate 11: the PAUSE half PASSES (PauseSim verdict=PAUSE_CONFIRMED/RESUME_CONFIRMED basis=state+clock via BackendControlState/ActiveBackendCount, both halves), the KILL half is NOT RUN (the seat's Stop-Process was refused by the permission classifier; STP-809's active-count path stays UNCONFIRMED LIVE). V13 00:15Z: C16 LIVE PASS - fired on the frozen 1-35 (no member > 50 m in 360 sim s, max 40.4 m) with a report-only TASKABRT before the timed end, silent on the moving 1-1; enable by Vrf__StallDetection=true (still default OFF). ~~**2026-09-21 STP-857 USER RULING (refines the TASKCMPLT-suppresses-TASKABRT rule above, for MOVE tasks only):** a MOVE task that reaches its armed end (timed completion) with NO displacement reports TASKABRT, not TASKCMPLT - found live on the Iron Storm drive, where two platforms that moved 0 m still had their C16 stall verdict suppressed by an already-fired timed TASKCMPLT. Implementation is a later lane.~~ **CORRECTION 2026-09-21 (U2 lane A, the same day; REVISED the same day after the owner corrected the first draft) - THE STRUCK SENTENCE ABOVE IS WITHDRAWN AS A "USER RULING". THE SYMPTOM IT DESCRIBES IS REAL AND THE OWNER AGREES WITH IT; WHAT IS NOT APPROVED IS THE PATCH AS SCOPED.** WHAT WAS PUT TO THE USER (session a7f6a276 transcript line 44947, the seat's own summary): *"What should the bus say when a move task's timer expires on a unit that never moved? The options are in the ticket. My recommendation is option 1: report an abort, 'armed end reached with no movement', where the current build reports complete."* WHAT HE ANSWERED (line 45180, a type=user record): *"857 as recommended - just make sure the task does involve movement. Not all do."* WHAT HE WROTE THE SAME DAY, UNPROMPTED (line 45457, a type=user record, quoted with his spelling): *"The rulling based on time is for tasks that do not include movement, such as defend in place and similar ones - units that are not expected to reach any other location. That's what you asked. 'A unit that moved 60 m of a 5 km leg gets a complete' is a new interpretation. If I agreed with that, I was tricked. ... The notion that geting stuck midway is a complete is completelly illogical."* WHAT IS WITHDRAWN, PRECISELY: only the "USER RULING" label. The recommendation he was answering presented timer-completion of a MOVE as already ruled; it was not. R4 is row 4 of the 2026-09-14 question table - in plain words, "when is a SECURE / OCCUPY / DEFEND complete: on arrival, after its Duration, or only when a later order supersedes it?" - and he answered *"4 given by the end time"* (line 28191). The case of a MOVE task whose timer expires was not the question asked, so that answer is not authority for it. See the SCOPE CORRECTION at `docs/experiments/TASK_VOCABULARY_ASSESSMENT_2026-09-14.md` R4. **WHAT THE OWNER ACTUALLY THINKS OF THE SYMPTOM** (owner, 2026-09-21, session c3b364bd, typed answer, his spelling): *"As for 857, a unit that is meanto to move to an objective and perform some action, but instead gets stuck in the middle of the way certainly did not complete, so not sure why you say the 'premisse was false'. My caveat was that you should not expect every task to require a movement, and abort in case the unit stays put."* So the symptom is real, and his caveat stands as he gave it. **WHAT IS NOT APPROVED IS THE PATCH AS SCOPED:** zero-displacement only, with completion semantics for units that DID move left unchanged - which would still report complete for a unit that moved part of the way and stuck. His own question on that point (line 45418, a type=user record) was never answered: *"Is this creating a special class of errors for no movement from the get go as opposed to movement that falls short of the objective?"* Branch `fix/reclamp-verify-and-movement-only` @ 3d58819 stays PARKED and unmerged. **HOW COMPLETION IS DETERMINED FOR A TASK THAT COMBINES MOVEMENT AND A DESIRED EFFECT IS UNDER THE OWNER'S REVIEW** (owner, 2026-09-21, session c3b364bd, typed answer): *"The notion of a hold-off or movement tasks seems to be rather fuzzy as described. Some verbs imply that the unit stays in place (e.g. to DEFEND). There are plenty of tasks where there is movement preceding the desired effect, like in 'ATTACK TO SECURE'. How are these distinguished? Seems to me that the time applies to the full task, including the movement and the achievement of the desired effect."* He has registered doctrinal and vendor-documentation research on it. Do NOT write "a MOVE completes on arrival", or "time-based completion is for non-movement tasks", as a settled rule anywhere. AS A SEPARATE STATEMENT ABOUT THE CODE: arming the Duration timer on every task with a Duration (746c091, 2026-09-14) is a code defect; the fix is gated on the owner's review of completion semantics (U1) and a PLAN gate. WHAT STILL STANDS, AS A MEASUREMENT ONLY: on run 20260921T114910Z the two platforms displaced 0.0 m and both C16 stall TASKABRTs were suppressed by an already-fired timed TASKCMPLT (as recorded in this row and RUNBOOK 11h; not re-derived by this lane). Separately: that run set `Vrf:DurationScale` 0.25, which put the 300 s armed end inside the 360 sim-s stall window. See `docs/CORRECTIONS_LOG.md` entry F-1. |
| 20 | Route pre-flight and its DELIVERY (user 2026-09-13, RL-20260913-01, archive: "the interface-side idea is very valuable ... users don't read logs" - he typed "Users dont read logs") | O (design) | At order receipt, before dispatch, walk each leg against the SAME public elevation and land-cover tiles the sim streams (validated to 3 cm; elev_face.py / lc2.py in the scratchpad are the prototypes) and the taskee's vehicle limits from the vendor's entity files; flag SUSTAINED faces (the measured discriminator: ~55 m windows above ~0.71 stop an M1A2 on sand; short pitches to 0.9 pass). DELIVERY, not logs: (1) STP task status - TASKABRT for a leg the vehicles cannot traverse, at order time (standard messages, no STP change); (2) STP map - an ObservationReport at the face's coordinates with the numbers, rendered by STP (their end; the channel to invest in); (3) the VR-Forces GUI - the segment drawn on the shared overlay via the remote controller's overlay-object API, for the audience. ORDER OF DELIVERY: map graphic and overlay as WARNINGS first; TASKABRT only after calibration against the legs where units DID pass in P11/G2/G3/G5 shows zero false alarms. Also fixes the +10 m systematic in the route-authoring heights (FINDING sec 1 note) while there. STATUS 2026-09-13 23:25Z: BUILT and COMMITTED 80831b9 (tools/preflight/leg_check.py + README + calibration record; the review's fixes applied and the gates reproduced by the supervisor in a clean checkout: selftest PASS, calibrate --verify-run 3/3 caught, 0/6 false alarms, margin 0.097) + docs/experiments/PREFLIGHT_CALIBRATION_2026-09-13.md. CALIBRATION as first written (2/2 flagged, 0/7 false alarms, margin 0.066 at a 55 m window, threshold 0.90) was REFUTED by its cold-start review (~23:15Z): the labels came from net displacement and missed that 4-27/2/1_A drove 22.7 km and then FROZE for 2,870 s, INSIDE the tool's own worst window (measured 22,697 m vs the window centre 22,702 m) - the highest "passing" ratio was a frozen unit, the margin was an artefact (0.025 corrected) and 0.90 missed it. CORRECTED (labels derived from the traces as closest approach to the next route vertex - units drive THROUGH vertex 1, so an end-position rule mislabels five movers; APPLIED): at a 40 m window the three frozen first legs score 1.098 / 0.990 / 0.966 vs the best clean pass 0.870 -> threshold 0.92, margin 0.096, 3/3 caught, 0/6 false alarms; at 80 m the separation dies (the window is an operating point). ALSO FOUND: a SECOND stop mechanism is active - 1-6 (P11) stopped on descending ground 480 m short of the flagged face and 856/HHC on flat ground; the pre-flight explains SOME stops, not the class (FINDING sec 7b). Wording must stay predictive ("PREDICTED IMPASSABLE"), never "this is why your unit will stop". The P11 probe type map goes into data/ for reproducibility. All tasks: 57 legs, 48 scored (9 sub-metre legs skipped), 14 flagged on 8 units in two clusters (the measured ridge; the southern approach to PL BLUE at ~34.33 N, incl. the ADA platoon at 1.24 because HMMWV-Avenger's max-slope is 0.60). Vehicle limits RESOLVED from the type map + vendor .entity files (parentFile inheritance; the P11 lifeform substitution reproduced). Found and fixed on the way: the land-cover tilesets serve only to L12 (154/165) and L10 (188) - an L13 request silently fell through to a coarser layer. Stated residual risk: one order, one terrain, three positives, 0.097 margin; the class->soil hop is ASSUMED for 7 of the 14 flagged legs (the flagged set is identical under paved-road 1.00 and sand 0.80, so the hop adds or removes nothing here) -> WARNINGS ONLY, as ruled. NEXT: wire it into the interface at order receipt (today a standalone tool with --c2sim-observations / --vrf-overlay outputs); 4-27's third positive CONFIRMED for its LEADER by the console read (FINDING sec 7c: same signature as 1-35; three followers crawled past the stopped leader at 0.30 m/s - the unit did not freeze, the leader did) BUILD ITEM B4 (REPORTING_ASSESSMENT_2026-09-14.md): port the scorer into the interface at order receipt (the product contains no Python, row 6); the emitter shape is already written in leg_check.py:1037-1121 (LocationObservation + NameObservation per flagged leg); STP's rendering of a LocationObservation is UNKNOWN - question 4 of DRAFT_STP_QUESTIONS_2026-09-14.md. FALSE-ALARM CANDIDATE 2026-09-14 (FINDING sec 7d): in G2 three re-formed members of 1-6 crossed the flagged T15 window 20-22 m off its centre on offset lines (0.41-0.43 vs the straight-line 0.744) and drove on; G3's leader froze 28 m from the same point. Offset-line scoring (the formation's lateral spread) is a registered follow-up for the tool. OFFSET-LINE ADJUDICATION 2026-09-14 (READ_4-27_G3_AND_OFFSET_SCORING) - CORRECTION 2026-09-21: this was headed "RULING". It is a SUPERVISOR STATEMENT, no owner words on file; all 276 of his messages were searched for /offset/i and /route line/i, 0 hits. It stands on its own merits and carries no RL id: keep the route line as the verdict (only rule with a positive margin under both readings); print the -50..+50 m slot-line ratios as a sensitivity range; the G2 crossings are not a false alarm (re-formed unit at 0.02x); the real false alarm is 4-27/T5 in G3 (leader's line 0.941, crossed) - one leg, logged, threshold unchanged; wording 'some of your vehicles'. |
| 21 | Customizations SURVIVE REINSTALLS of C:\MAK (user 2026-09-13, RL-20260913-02, archive: "we need something that survives reinstalls") | O (design, docs read 2026-09-13) | **NOW:** V7 CONFIRMED (PREREG_V7_AO20_2026-09-15.md, 21c1430): the western mesh refusal is the OVERSIZED nav area's generated data (MojaveCOA 5.5x over UG52's documented 20x20 km maximum), not a missing customization - product rule adopted: cap every generated area at 20x20 km, tile the AO, fail generation on any sector with connectivity ratio < 0.9 (STP-802/803); the SMS/appData customizations already built stay the mechanism. Vendor mechanisms per kind: (a) platform files (.ope, the navigation-profile binding) and movement definitions (.sysdef, slope-avoidance-factor) -> an INCLUDING custom SMS that holds only the changed files (Migration Guide 1.2.2 p12 "Custom SMSs that include and extend a VR-Forces SMS are much easier to upgrade"; UG52 68.3.1 p1310 higher-priority SMS overrides; 68.7.1 p1323), kept in our repo; (b) generated navigation data -> the generator's --navDataDir / --outputPath (observed) and the sim's --userDataDir (UG52 Table 11 p184) - so the data lives at a short root WE own outside C:\MAK, referenced by the terrain copy; (c) terrain copies (.mtf) -> loaded by absolute path (UG52 -T p184; our fixture already does this); (d) scenarios -> absolute path (-L, UG52 p182; the launcher passes a relative userData path today - one-line change); (e) navigationProfiles.mtl and vrfSim.mtl -> NO dedicated redirect; the vendor's own prescribed method is copy-forward of ./appData, ./data, ./userData (UG52 3.7.1 p126), so a changed profile ships in our repo with a deploy step that copies it into place with backup + diff; --appDataDir as a no-touch alternative is UNCONFIRMED for this file (a one-launch test if ever needed). DECISION TAKEN (follows from the user's requirement): the navigation data's permanent home is a short root outside C:\MAK (C:\C2SIM\vrf-nav\), NOT the vendor's userData\navData (whether a reinstall preserves userData is not stated). Nothing under C:\MAK but the deployed fixture and, only if G6 fails and the user sanctions it, one settings file STATUS 2026-09-14 00:01Z: the nav-data home is REALIZED - C:\C2SIM\vrf-nav\navData\MAK Earth (online)\ holds the full-COA area (26,525 files, 1.2 GB, generated 3.3 h headless), its runtime config points there, the terrain COPY in tools/navdata/out registers it, and C:\MAK holds 0 nav entries; the .mtf copy and the deployed fixture are the only things that need re-creating after a reinstall (both scripted: make_nav_terrain.py + build_fixture.py). USER RULING 2026-09-14 (RL-20260914-01, archive; stamped 11:15:53Z in the record, not the "~12:40Z" this row used) ("Ok on 2-3"): if the custom including SMS cannot live outside C:\MAK, its placement under C:\MAK is SANCTIONED; relocating appData via --appDataDir (UG52 Table 11 p178) for the G8 gamewareMemorySize variable is SANCTIONED if G8 is needed. CUSTOM SMS HOME REALIZED 2026-09-14 ~13:00Z: C:\C2SIM\vrf-sms\ (sibling of vrf-nav) holds C2SIM_EntityLevel_AbstractGraphs.sms - includes the shipped EntityLevel.sms unchanged and overrides ONE script by script id (UG52 68.3.3 p1312 priority, 68.3.4 p1313 'scripts in the highest priority SMS supersede', absolute paths UG52 Table 15 p271 / 12.2.1 p352); fixture R9_Mojave_Empty_52_NavAO_AG references it by absolute path (FixtureGen --sms, commit 5131f17). Run-time proof owed: the override's printInfo line in a member console. G8 APPDATA RELOCATION BUILT, committed on main a1f3b7d 2026-09-14: C:\C2SIM\vrf-appdata\appData byte copy with cache/data/userData junctions, one change loadAllNavigationDataOnTerrainLoad 1; LaunchVrf52 -AppDataDir; record docs/experiments/APPDATA_RELOCATION_2026-09-14.md; runner passthrough -VrfAppDataDir / --vrf-appdata-dir being applied now; validation launch owed (caveat: --appDataDir shares the option-parsing path with --logFileName, the 1-in-3 startup crash - if the launch dies at startup, drop the option once first). G7 ATTEMPT 4 MEASURED 2026-09-14 15:42Z (run 20260914T154243Z, 009c674): gates 36/36; legs 1 (1 seam) and 2 (4 seams, up to 1,994 m) mesh-planned 4/4; leg 3 (10 seams, 4,914-5,048 m) refused 4/4 once each, feature planner drove it to arrival. Reading B FALSIFIED (connected across seams); ceiling (1,994, 4,914] m is AREA-dependent (AO20 planned 10.1 km) = reading A -> G7b abstract graphs (fixture _AG; order adds a 4th task re-issuing the long leg after arrival, CR2) then G8 budget/memory via the relocated appData. Timing is not a discriminator. LAZY LOAD CORRECTED: async ~237 s / 2.3 GB stream from PLACEMENT; the 240 s settle covered it with 9 s margin; appData load-with-terrain validation next (appdata_val launch line ready: stock fixture, --vrf-appdata-dir, AtOrder, no settle, 300 s). G6/G7 CLOSED 17:15Z: the mesh refusal on the 41x54 km area was the vendor script's hard-coded useAbstractGraphs=false (ground-vehicle-move-to.lua:488). G7b (run 20260914T170824Z, custom including SMS C:\C2SIM\vrf-sms\C2SIM_EntityLevel_AbstractGraphs.sms) planned the 4,989 m / 10-seam leg 8/8 (28-35 abstract points) with 0 refusals; the same leg was refused 4/4 (attempt 4, stock SMS), 8/8 with gamewareMemorySize 128 (G8, run 165919Z) and again with gamewareQueryTimeBudget 50 ms (G8b, run 172134Z). Memory and time budget are NOT the lever. The custom SMS is product configuration (FixtureGen --sms default; STP-804). appData validation (run 164906Z): with loadAllNavigationDataOnTerrainLoad 1 the first goals pass both gates under AtOrder with ZERO settle (attempt 3 failed 18/18); the placement-triggered 2.3 GB stream persists and is cache-bound (237 s cold, 15-20 s hot) -> warm the cache in the prepare step. OPEN: whether abstract-graph paths avoid the ridge faces (FINDING_EARLY_STOPS) - 1-35 lane test with the custom SMS owed. Full harvest G7B_G8_RESULTS_2026-09-14.md in progress. CORRECTION 2026-09-14 (harvest G7B_G8_RESULTS_2026-09-14 sec 1.5/3/3.1, records executor): the appData validation sentence above is WRONG and is WITHDRAWN, not deleted - run 164906Z had 8 current-point gate failures (`fail in action Is current point in nav area?`), and every tank's slot move and first leg ran on the FEATURE planner because the order reached the bus 4.7-7.7 s before the area row; loadAllNavigationDataOnTerrainLoad 1 is a NULL RESULT (the 2.3 GB stream is placement-triggered at 1 and at 0 alike) although the relocated tree WAS read (19 settings files rewritten per launch, one-to-one with --appDataDir). Unusable window = first entity placement -> first `New Primary nav area` row from a placed platform: 9.1-12.1 s warm, 237 s cold. DEMO-FLOW RULE: gate PushOrder on that row (runner item, owed), warm the cache in the prepare step, CreationPolicy=AtInit so members are placed before the wait; a fixed settle if used: >= 30 s warm, 240 s+ cold. G7c-gate 2026-09-14 19:08Z (run 20260914T190751Z, -PreOrderGate NavArea, same tree and policy as run A): SINGLE-VARIABLE confirmation of the custom SMS as the fix for the long-leg refusal - 0 gate failures, 32 gate successes, 32 mesh plans, 8/8 long legs planned (28-35 pts outbound, 33-35 pts return), 0 refusals; CR5 (the settle/AtInit confound) and run C's appData confound are both removed, the only difference from refusing run A is the SMS override. Ready gate FIRST LIVE USE fired at +118.1 s on a COLD file cache (the `New Primary nav area` row); a companion run G7c (20260914T185945Z, same fixture, no gate) found the cache cold again within an hour of build/review load and was VOID for the SMS question (nav area never registered, 31/31 goals gate-failed). Demo-flow implication: the cache goes cold within about an hour under build load - the prepare step must warm it immediately before the demo and the READY GATE, not a fixed timer, is the control. DOCS PASS 2026-09-14 20:00Z (NAVDOCS_ABSTRACT_GRAPHS_AND_SLOPE_2026-09-14.md, being committed by another executor): the flat A* query has a documented 200 m propagation box (Autodesk AStarQuery option; per-platform propagation-box-extent in Ground_Vehicle.ope) - the successful abstract routes deviated 231-292 m, outside it; abstract graphs are documented COST-BLIND; the ridge face is legal to the mesh (slope-max 46 deg, cost no-go unreachable at factor 1.0, soil invisible), so no planner flag routes around it - the documented lever is slope-avoidance-factor. PLAN CHANGE: the ridge test is replaced by N1 (propagation-box-extent 2000, stock Lua, the G7 route; PREREG_N1_N2_CORRIDOR_SLOPE_2026-09-14.md being written) then N2 (+ slope-avoidance-factor 2.0 on the 1-35 lane); N3 (the abstract-graph SMS on the ridge) only if N1 refuses. FixtureGen's abstract-graph-SMS default stays PROVISIONAL pending N1 (STP-804 comment). N2b 21:05Z: the abstract-graph override is NOT sufficient by itself - members created AtOrder 22 s after the init's own 1-35 proxy was placed in the same cell got 0 points for every goal 0.3-5 s after placement, 5-10 s after the run's first area row (5th ridge freeze); N2c (300 s task delay) REFUSED identically -> not a timing problem: the readiness gate is fine as a load signal; the 1-35 destack-start POSITIONS themselves refuse every mesh query (hole/island candidate) - a scenario-prep check (is every placement point on the walkable mesh?) belongs in STP-804/806. N2d 23:05Z: from a start 500 m along the leg the unit drove the AUTHORED V0->V1->V2->V3 line to within 0.85 km of V3 - 1.3 km north of the six-run freeze line; the freeze is a LINE property and the pre-flight tool's lateral scoring is the remedy (shift the leg to the cleared side). Mesh queries for long goals still fail region-wide in the west (abstract-graph connectivity suspected). 2026-09-15 11:36Z WEST20 CONTROL (0b765a2): area SIZE FALSIFIED as the cause - a fresh compliant 20 km area over the western ground is as fragmented as COA (234/1,598); the terrain input at generation time is the variable; the offline connectivity-ratio gate (>= 0.9) is the LOAD-BEARING control and generation is not idempotent on a streamed terrain (record the terrain state per area); AO20's clean data is a dated artefact. V7's live result stands. AO20-extents regeneration (the falsifier) running. STATUS 2026-09-25 (after the machine rebuild lost C:\C2SIM\vrf-sms): the four derived SMS trees are now BUILT FROM THE REPO by tools/sms/Deploy-C2SimSms.ps1 (commit f47fe0b) from the installed vendor files - no vendor file is committed, every patch asserts its recorded original line first - rebuilt and offline-validated 2026-09-25 (tools/sms/README.md; G1/G2: the original .sms text, printInfo line and hashes were never recorded, so the rebuild is from the record, not byte-proven); run-time proof line not yet observed on the rebuilt trees. |
| 22 | Runner robustness: a run must never leave the sim and the interface running (found 2026-09-14: the G6 runner pwsh was KILLED from outside at t+127 s of its window - TerminateProcess(-1) signature, every crash path refuted by measurement, killer unidentified; its in-process finally could not run; sim + interface ran 9 h, 5.9 GB log) | B (built + committed f6e68d9 2026-09-14 ~12:30Z; verified by the supervisor: parse, 64-bit gate fires under the 32-bit host, dry run exit 0 under the 64-bit host; FIRST LIVE USE = G7) | **NOW:** the runner stop rule is FIXED and MERGED (ff15a4e) - it closed a run window live at +184 s of 900 (V13); tools/PauseSim added as the ELEVENTH 5.2 consumer (merge f13df1b) for the V5 Q5 pause+kill probe. GATE G-A RERUN DONE (a9b8c2c): VrfBridge.dll re-pinned at main 5881b7d, hash E3F40524... (997,376 B) - all ELEVEN consumers one hash, 19/19 suites, rulings 176/0, routeshift 61/0; the 99B7B235/165e04c pin is SUPERSEDED. docs/experiments/RUNNER_EXIT127_2026-09-14.md. Fixes in flight: (1) 64-bit gate - bare `pwsh` resolves to the 32-bit 7.6.1 on this machine's PATH, which undid the 2026-09-07 mitigation silently; (2) teardown marker runner.teardown-ran + an OUT-OF-PROCESS wrapper backstop (scripts/RunScenario.sh: stdout to a FILE, no tee/pipe - a child inheriting the pipe held the supervisor's harness open for nine hours; StopIface + StopVrf52 + observers.stop if the marker is absent); (3) the status line tail-read 256 KB and printed '(no samples)' on a 1 GB trace - replaced by a backward block scan; (4) the loop re-read the whole app log every 5 s - incremental reader. Deferred: a detached watchdog for the case the wrapper dies too. Rule: no Stop-Process sweeps during a run window; cleanup excludes the runner pid. A2 RUNNER WATCHDOG + PRE-ORDER SETTLE DONE, committed on main 374ea49: scripts/RunnerWatchdog.ps1 detached process; -PreOrderSettleSecs / --pre-order-settle; explicit -WatchSecs does not grow with the settle - the runner warns; tested offline a/b/c; NOT yet exercised inside a live run. FIRST LIVE USE of the watchdog clean (armed Stage 3w, 750 ms survival, two-observation confirm, stood down on teardown-ran; wrapper used the run-dir pointer). DEFECT: -StopWhenComplete never fires - condition (4) needs an RPT trace line that no run has ever produced (0 in 5 traces); fix = accept the taskee's post-TASKCMPLT C2SIM position report (STP-794). Licence: c8730e7 resolves MAKLMGRD_LICENSE_FILE User-then-Machine per process; expired-licence pre-launch check. Probe runs 4-7 of the day all tore down cleanly with the watchdog standing down each time; runner fixes in progress (STopWhenComplete on C2SIM position evidence, sampler sized from the derived window, relocated connection-config path, clock labels, licence resolver in the 5.0.2 launcher). READY GATE -PreOrderGate NavArea (7531bc6) fired live at +118 s; the stop rule closed windows early in two runs (the C2SIM-capture satisfier is live via ListenReports incremental capture, 0999eeb); eleven clean teardowns today. GATE G-A 22:45Z: the merged main is built and PINNED (RUNBOOK sec 9 pin line): bridge 99B7B235..., six 5.2 consumers one hash, suites green; four tools remain 5.0.2-only until a ruling. The 09-07 deployed exe is superseded - every run from 22:45Z is on the merged build (rulings, reporting, sim clock, heading/speed, pre-flight port). 00:10Z: runner stop rule FIXED (ff15a4e) - it never fired since B1 changed the log line; STP-809 merged (4cd84d7) so the G-A pin 99B7B235 is STALE until the rerun; V13 (reporting + watchdog) running on the pin. V13 00:15Z: the fixed stop rule CLOSED the window at +184 s of 900 (1 TASKCMPLT + 2 TASKABRT = 3 terminal of 3) - first live closure; 15 clean teardowns. |
| 23 | REPORTING gaps (assessment 2026-09-14; user: "Start looking into reporting in parallel") | O (B5/B3/B1/B2/B8 BUILT on feat/reporting + cold-start reviewed; LIVE CONFIRMATION OWED) | docs/experiments/REPORTING_ASSESSMENT_2026-09-14.md. G6 on the bus: 14,698 bodies (102 NameObservation + 14,596 Position + 0 TaskStatus in the 1,200 s capture; 8 TASKCMPLT in 9 h, ALL from arrival evidence C15 - the 3 vendor completions were swallowed; 34 of 42 tasks never got a status; the first status came 1,659 s after the order; 129 pushes failed silently; 1 of 128 units (the platform-mapped brigade 2/1_AD/25_) never reports a position because VR-Forces truncates its marking to 10 chars and our lookups are by name). Ranked gaps G1-G11 and build items B1-B10 in the record. Decided: TASKABRT for stalls (user), TASKABRT for refused/skipped tasks (supervisor, user may override). Queued: B4 pre-flight port, B7 heading+speed (native), B9 bundle run, B10 five questions to STP (DRAFT_STP_QUESTIONS_2026-09-14.md - the user sends). JIRA: epic STP-777, items STP-778..787. STP-SIDE FACT 13:10Z (STP_PARSE_CHECK_2026-09-14.md): our bodies parse on every SDK STP pins, but STP's C2SimBridge ParseReportContent consumes ONLY PositionReportContent - TaskStatus and ObservationReport are parsed and DISCARDED; bundle shape A (ours) is fine, shape B silently loses all but the first. So TASKSTRT/TASKABRT and the pre-flight observations need an STP-side change to be seen (Jira on the STP side). STATUS 2026-09-14 ~13:00Z: B5 (9286398), B3 (ebf8da9 + f0d1c68), B1 (81d108c), B2 (f057fc2) and B8 (4e9a9ce) are BUILT and committed on feat/reporting; cold-start review verdict MERGE WITH FIXES (2 MAJOR latent, both in NameRegistry, plus 11 minor/notes) and every finding applied on the same branch. Offline self-tests are the only evidence; FOUR LIVE GATES OWED, all from one COA-STP1 run - 128 sent / 0 skipped, 14 TASKSTRT within seconds of the order, zero "Failed to deserialize" lines, more than 102 NameObservations. NATIVE success() FORWARDING DONE 2026-09-14 on feat/integration (feat/heading-speed exposes TaskCompletedEventArgs.Success from DtTaskCompleteReport::success(); OnVrfTaskCompleted now reads it instead of a hardcoded true, so a vendor-reported FAILURE is TASKABRT, releases no successor and cancels a parked engage; --report-selftest walks both flag values to the wire; G2 closes on its live gate). ONE ITEM STILL OWED: STP's behaviour on a duplicate ReportID (B2's retry is at-least-once - added as question 6 of B10). BEHAVIOUR CHANGE: an advance-then-engage task now reports TASKINPRG at the MOVE's completion and keeps its one TASKCMPLT for the engage, so STP sees TASKSTRT -> TASKINPRG -> TASKCMPLT for ATTACK/BREACH with a resolved target. B10's question count is now six. |
| 17 | DI-Guy character data (makData19 DI-Guy package, 6.33 GB) | D | Found 2026-09-06 (PREREG_COASTP1_52_RUN1 sec 6-7): without `C:\MAK\SharedData\19\latest\ModelData\Lifeforms\DIGuy` the 5.2d headless sim crashes (0xC0000005, DtDiGuyController::determineInitialHandItem) at the first DI-Guy human it creates - i.e. at the first infantry / mortar / CSS / fire-support unit the fidelity table maps. A demo whose ORBAT has any infantry needs the package installed first; the interim (vehicle-only proxies for lifeform rows) is a fidelity regression - CORRECTION 2026-09-21: this said "= user ruling". It is a SUPERVISOR STATEMENT with no owner words behind it (RL-UNVERIFIED-DIGUY01); the one real fidelity ruling, RL-20260902-01, is about unit TYPE MAPPING, not DI-Guy. **2026-09-20:** the package is ON DISK (`Lifeforms\DIGuy`, 6.9 GB, directory dated 2026-09-06) - status was V: one run creating a DI-Guy human is owed before a dismount ORBAT is demoed. **2026-09-20 D1:** row SATISFIED (d1_harvest_report.md sec 10) - four dismount humans named by the sim's own console (R 9, R 10, AT4 9, AT4 10, all under DtHumanMoveToController), 172 position samples each, ~1200 m walked, backends=1 on 203/203 samples throughout, no crash artifact (no .callstack.log, no .dmp, no 0xC0000005) over 376 s of exposure since their first appearance. Caveat: that `DtDiGuyController::determineInitialHandItem` specifically executed is INFERRED, not read from the (secret-bearing, unopened) vendor sim log. |
| 24 | Slope-stop remedy: the pre-flight LATERAL ROUTE SHIFT (STP-804/806; the remedy for row 20's flagged legs and row 19's stalls - STP authors a straight leg across a ridge face the vehicles cannot climb, and the interface drove it literally) | **PROVEN LIVE 2026-09-15**, default OFF pending a product ruling (the user's call) | **NOW:** three runs on 1-35's ridge leg, one anchor, one variable. V8 (023743Z) ACTED but shifted 125 m SOUTH - a pre-registered STOP; cause found and reproduced offline to the digit (C2, the formation band, was choosing the SIDE, which the design says it may never do; missing tiles FALSIFIED as the cause - 0 NaN samples across the whole search band) and FIXED by a two-phase chooser (C1 alone picks the side, C2 may only size the shift on it) plus "unknown is never clear", merged c5f166d. V8b (033226Z) on the fixed binary shifted +250 m NORTH (ratio 1.248 -> 0.761, band 0.862, every candidate nan 0) and **6 of 6 vehicles crossed the ridge at ~10 m/s, past V1, no crawl** - including M3 1, the slot that froze in V8. V8z (041036Z), the ZERO-OFFSET CONTROL (the same four waypoints on the authored line, shift OFF), **froze 5 of 6 members 8-39 m from the P11 point** with under 2 m of movement in the final 600 sim s, on the same mesh-planned hops and with zero planning refusals. ATTRIBUTION: the LATERAL OFFSET is the remedy; waypoint insertion is the delivery mechanism and is inert on its own. The sampler also located the face - the V8z units stopped 0-52 m SHORT of the flagged window, reproducing the measured toe distance behind PadMeters 50. Bound: n = 1 per condition, one leg, one terrain. Offline gates: --routeshift-selftest 84 ok / 0 fail with 0 network fetches, suites 19/19. OWED: the product ruling on flipping `Vrf:PreflightRouteShift` to ON (do not flip it here), and more orders before the threshold's false-alarm rate is anything but "nine legs of one order". Evidence: docs/experiments/PREREG_V8_ROUTE_SHIFT_2026-09-15.md (RESULTS for all three runs + ATTRIBUTION) and docs/experiments/DESIGN_ROUTE_SHIFT_2026-09-15.md sec 4.2a/7a; STP-793. **2026-09-20:** default ON merged f26d4ad; harmless-on-R9 run D3 registered. **D3 2026-09-20:** default ON proven harmless on the R9 order live: 0 shifts, 0 fetches, ~4.5 s one-off deferral; shift firing on a flagged leg still rests on V8b. |
| 25 | Back-end LIVENESS + nav data as a precondition for ground tasking (V6 lane, 2026-09-15) | O | **NOW:** dispatching the R5 order (platoon + COMPANY + tank move-alongs on the R9 lean init) STOPS the VR-Forces 5.2d back end 121 s later on BOTH the plain fixture (V6d) and the nav-data fixture (V6e MISS 14:02Z; STP-823 reopened - 'no nav data' was a coincident symptom; the ridge order on A4/V5 runs fine; the company move on a composed unit is the leading candidate, V6f decides) and the interface never notices - it re-reads BackendCount only on the task-clock stale branch and kept sending position reports off stale data (STP-822). Demo rule: every ground order runs on a fixture WITH nav data behind the nav-area READY gate. Owed: an interface refusal for ground tasks without a nav area, a timed liveness read with TASKABRT/ObservationReport on loss; A4 (V5's fixture) is the control. **2026-09-15 15:35Z:** STP-822 liveness MERGED (main 3c71025, default ON; live confirmation still owed). V6f (which R5 task triggers the runaway) NOT yet run: three launches died at the sim's federation CREATE on the long-lived rtiexec (STP-825; JOIN-path holder workaround confirmed, RUNBOOK 9c; rtiexec restart = user's call) and one at Stage 8 on an SDK protocol-detection defect tripped by the order file's comment (STP-830, data fixed). Demo impact: the runner must hold the federation for the sim (product follow-up) and order files must keep comments free of '<Name' tokens. **17:00Z:** V6f harvest: the runaway is triggered by an order whose waypoints are on another continent (R5 order = Sweden; units = Mojave) - the interface dispatched an 8,769 km leg; STP-833 (refuse out-of-terrain routes as MALFORMED) is the demo-relevant product rule; V6g (Mojave coordinates) confirms. STP-822 liveness LIVE PASS. **17:45Z:** V6g HIT - with Mojave coordinates the three tasks complete and the back end stays healthy: the runaway was the order's off-terrain waypoints (STP-823 retitled). Demo rule: STP-833 pre-dispatch route check; STP-837 arrival-evidence fix. Row 25's engine risk is CLOSED; the launch-side STP-825 workaround (runner Stage 2h) is the demo posture. **D3 2026-09-20 CORRECTION:** the rtiexec's lifetime refusal rate is ~42% (19 successes/14 refusals as of 20:22Z, then 5 more refusals + 1 success), clustering in runs of several - Stage 2h's four attempts ALONE are NOT demo-grade against a cluster that size; the demo posture is now a PERSISTENT HOLDER started once and retried until it joins, held for the day (RUNBOOK sec 9c). **2026-09-20 three-arm RID experiment:** no RID lever (REFUTED); cause narrowed to an rtiexec buffer defect (its own log text lands in the FDD image); persistent holder remains the posture. **2026-09-20 notify-level test INCONCLUSIVE-UNDERPOWERED (0/30 at -n 0 vs 5/30 at -n 3, p 0.026; level-3 instances have also run 0/45); posture unchanged: persistent holder; rtiexec restarted today (leak cleared).** **18:35Z:** STP-833 (route-extent refusal, default ON, bounds 50 km leg / 100 km vertex - COA-STP1's worst is 45.4 km) and STP-837 (arrival evidence requires traversal) BUILT and merged (RUNBOOK 11c/11d); live exercise owed on the next run. **18:50Z:** STP-833 LIVE PASS (V6i, run 184048Z: the Sweden order refused before dispatch, 1 TASKABRT + 1 ObservationReport, 0 dispatches, ws flat); STP-823 Done. The demo order is protected against off-terrain geometry. **2026-09-21 NOTIFY2 replication:** 0/96 at -n 0 vs 14/96 at -n 3 but registered VOID-INSTRUMENT / indeterminate by design (instance heterogeneity); -n 0 NOT adopted; posture unchanged and sufficient: persistent holder + -n 3. **2026-09-21 D5b, TWO NEW OPEN ITEMS (Way B rehearsal, not yet closed):** STP-853 - the interface reported a dead back end as healthy for 2 min 38 s (60 position reports at 4 units' frozen coordinates, none marked stale) and dispatched a task into it 10 s after it died; the existing STP-822 detection chain (~118 s ageing + 40 s rule) took 158 s total to notice. STP-854 - the Way B back end itself crashed 1.3 s after the order arrived, n=1, undiagnosed, not the known startup crash; a confirming run (C1) is registered to isolate cause from the harness before or alongside C2. Neither item is specific to Way A or the nav-data lane this row otherwise covers; both are demo-relevant and OPEN. |

## What this milestone does NOT include
GUI automation of any kind; the harness features (ledger, observers, scoring, StopWhenComplete);
golden-parity to 5.0.2 (ARCHIVE).

## Exit criterion
An operator following DEMO_RUNBOOK.md, with no repo tooling beyond the three start scripts,
brings up rtiexec + VR-Forces (GUI) + the interface, pushes the R9 init and the R9 order from
STP (or PushInit/PushOrder as the STP stand-in), and sees the three units created and moving in
the GUI and their position + task-status reports in STP - twice in a row after a reset,
without restarting VR-Forces.
