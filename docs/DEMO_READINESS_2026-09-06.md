# DEMO-READY: the interface as a standalone deployment (no harness)

User ruling 2026-09-06: "When the time comes for tackling the real tasks, not just move along,
let's first get the app ready for deployment as a demo, working without the harness for this
bit." SEQUENCE: (1) finish the current move-along / scale measurement (COA-STP1 on 5.2 - since
2026-09-06 22:00Z: 11 taskees with order-time materialization, C13), then (2) THIS milestone,
then (3) ADDED 2026-09-06 ("Add to the plan as suggested"): the aggregate-level profile, Y-15
realized - docs/PLAN_AGGREGATE_LEVEL_PROFILE_2026-09-06.md, then (4) the STP task vocabulary
(goal-full-stp-task-vocabulary) targeting both model sets.

"Without the harness" = the operator starts VR-Forces and the interface by hand (or by ONE thin
start script that only sets the interface's own environment), STP pushes the init and orders to
the C2SIM server, the interface creates the ORBAT and executes the orders, and STP / the
VR-Forces GUI show the result. No runner, no WatchVrf, no ledger, no scoring - those stay the
TEST harness. The GUI is the audience's window (a human watching), which is not GUI automation
and does not contradict the headless goal; the interface itself stays headless.

## Gap list (D = done and verified today or earlier; O = owed; V = exists, needs one verification)

| # | Requirement | Status | Evidence / what is owed |
|---|---|---|---|
| 1 | ORBAT loads faithfully (declared tree composed; coarse leaves expanded; leader = declared first) | D | N1/N2/N3 2026-09-06: PREREG_N1/N2/N3; DESIGN_ORBAT C1-C11 |
| 2 | Units MOVE as units on 5.2 (company) | D | compose 3/3 x5 runs; C1b closed by the console. AT SCALE 2026-09-07 (PREREG_ASSEMBLY_LAYOUT): the 11-unit COA, spread on 700 m rings (C14), drives 24-33 km legs and reports the first two TASKCMPLT at scale; demo overlay carries CreationPolicy=AtOrder + DeStackCreates/700 + TaskPredecessorTimeoutSeconds 7200. The 9-hour "no completion" question is CLOSED by C15 (arrival-evidence completion, verified P9). FOUND 2026-09-07 (3f): STP's routes start at the unit's own position, so spread units marched back into the assembly point and jammed there - Vrf:DropOriginVertexMeters (default 100) drops that leading vertex; verification run P11 |
| 3 | Task-status (TASKCMPLT) reports to the C2SIM bus | D | every run since R9; AT SCALE 2026-09-07: two vendor completions per spaced run (003457Z, 103246Z) after 24-29 km legs; the interface's own arrival-evidence completion (C15) covers units the vendor holds behind a straggler - VERIFIED run 114026Z (P9): 4 evidence completions incl. 1-35 with a stuck M3 (4 of 6 within 500 m), 3 later vendor completions swallowed, one TASKCMPLT per task, no false report |
| 4 | POSITION reports to the C2SIM bus on 5.2 | D | R1 (PREREG_R1) and AT SCALE 2026-09-07: run 103246Z captured 69,314 PositionReports over 45 min (127 units every 10 s); appsettings.Demo.json PositionReportSeconds 10 |
| 5 | appsettings.json DEMO defaults | V | `src/VrfC2SimApp/appsettings.Demo.json` (2026-09-06): the 5.2 profile keys the runner injects (ConfigFileIdentity, Federation "", FedFileName "", ConnectionConfigFile MAK-ONE-2025, TypeMapFile 52, VrfHome) + ClientId STP (override per init), compose ON, PositionReportSeconds 10, console levels -1. Loaded by DOTNET_ENVIRONMENT=Demo (HostApplicationBuilder default overlay); csproj copies it. Owed: one standalone start with it (row 7's run) |
| 6 | Runtime environment without the runner | V | IN-PROCESS since 2026-09-07 (user: "I need C# code I can send to the solutions engineers"): `src/VrfC2SimApp/MakRuntime.cs` reads Vrf:VrfHome/VrLinkHome/RtiHome/RidFile/RtiAssistantDisable from appsettings and prepares the process itself (PATH prefix, MAK_*DIR, RTI_RID_FILE, RTI_ASSISTANT_DISABLE) before the bridge loads; the rid ships beside the exe; a configured value overrides a differing process variable (logged; a stale machine-level MAK_VRLDIR=5.8 on the dev box was the case that decided it), empty keys leave the environment alone. The deliverable = the published folder + appsettings; `scripts/StartInterface52.ps1` is now optional convenience (it still selects DOTNET_ENVIRONMENT=Demo and the per-run overrides). The product contains no Python; Python is test-harness analysis only. `VrfC2SimApp --runtime-check` is the deployment smoke test (applies the bootstrap, binds the bridge, prints the stack, exits; negative control with wrong roots FAILS loudly - verified 2026-09-07). FOUND BY IT: the host resolved appsettings against the CURRENT DIRECTORY, so an exe started from any other folder ran without its settings (the runner and the script set the cwd, which hid it) - FIXED: ContentRootPath = the exe's folder. Owed: one standalone start on the deployed build |
| 7 | VR-Forces GUI ON + standalone start | V | every 5.2 run today was -NoGui and runner-started. ONE end-to-end standalone run owed: StartRtiExec52 -> LaunchVrf52 (GUI) -> StartInterface52 -> PushInit/PushOrder as the STP stand-in; the interface path is expected unchanged (UG52 4.1.2 independent mode; the GUI is a second federate) |
| 8 | ApplicationNumber without the ledger | D | demo block 9101-9199 in appsettings.Demo.json (outside the test ledger's 4xxx range); `-AppNumber` on the start script; "two interfaces on one network must differ" documented in the overlay and the runbook |
| 9 | Reset between demo runs | V | tools/ResetVrf (RUNBOOK sec 8) exists; verify a clean "reload scenario -> push init again" cycle without restarting VR-Forces (duplicate-init guard exists: VrfC2SimService.cs:577) |
| 10 | Terrain + real-time fixture | V | MAK Earth (online) needs internet; an offline fallback terrain (a shipped SharedData terrain) should be named in the demo runbook. Real-time fixture BUILT 2026-09-06: `tools/FixtureGen/frame_variants/R9_Mojave_Empty_52_RT.scnx` (build_fixture.py --profile 5.2 --empty --frame-mode variable-frame --out-name R9_Mojave_Empty_52_RT; frame-time 0.1; validate_fixture.py passes every structural check and fails only its two FFRTC-pair assertions, frame-mode/frame-time, by design). Owed: the sanctioned deploy to C:\MAK\vrforces5.2d\userData\scenarios (same command + --out-dir) and one run |
| 10a | Cross-country traversability (2026-09-07, PREREG_ASSEMBLY_LAYOUT 3e) | O (user) | Units stall silently on the cross-country approach from their slot and on fixed spots of the routes (a grade above the vehicle's max-slope; the vendor's obstacle planner ignores slope, UG52 23.5, and military vehicles ignore roads by default, 40.54). The vendor's traversability-aware planner is NAVIGATION DATA (UG52 ch 66: a NavMesh per navigation profile - slope-max, step-max, entity radius - generated from the GUI for a navigation area; "included with VR-Forces terrain databases", generating it "requires an additional license", 5.x). MAK Earth (online) has none for the Mojave AOI. Options: (a) generate navigation data for the demo AOI - FEASIBLE HEADLESS, verified 2026-09-07 17:00Z: bin64\vrfNavGenerator.exe (the process the GUI spawns) generated a 3x3 km ground-platform area in 47 s under the DEMO licence (PREREG_ASSEMBLY_LAYOUT 3g RESULTS; traps: MAX_PATH, --outputPath is emptied first, --navDataDir must be given); loading = a navData record in a COPY of the terrain .mtf (UG52 66.5); 20x20 km per area, several areas per terrain (66.2) - the COA's 39x52 km route box needs ~6 areas; live gate = the probe run on a 20 km area; (b) Prefer Roads for the tasked units (DtSetNavigationPreference set-data, UG52 40.54); (c) the aggregate-level profile, which has no vehicle dynamics. MEASURED 2026-09-13 (PREREG_NAVDATA_G3, three runs compared at EQUAL simulated time): navigation data over the assembly ground gives the COA NO speed effect (0.98x of the no-mesh run at sim 1170 and 2500), slightly tighter formations, and does NOT free the early stops - 1-35/2/1_A froze at 1.97 km in all three runs with the slope-aware planner active over it, 1-6/2/1_AD stopped in three different ways. The slope-blind-planner explanation of those stops is REFUTED for 1-35. The G2 "engine collapse" was a concurrent agent load on the machine, not the mesh (G3: steady 1.6x, ~5 cores). What stops those units is the open question. CONSOLES READ 2026-09-13 (FINDING_EARLY_STOPS_2026-09-13): the sim reports the move as running / unblocked / goal unchanged forever, enters no failure branch, and the units freeze on near-level ground just past a crest after climbing 41-42 deg pitches; the slope explanation is REFUTED as stated (grade at the stop -1 to +6 %; a same-type unit crossed the ridge 400 m away); G5 (1-35 alone, console debug level 4) RAN: the freeze is intrinsic (same point to 1 m alone), and the console prints commands, never the vehicle's response. VENDOR PASS (FINDING sec 6): a vehicle that stops while its task runs is UNDETECTED BY DESIGN in VR-Forces 5.2 (base give-up test "always returns false"; the decideToGiveUpTask SAMPLE shows the integrator adding the test); soil under the stop read from the streamed tiles = sand (0.80), not water/muck; CAUSE (FINDING sec 7; cold-start reviewed 2026-09-13, STANDS WITH FIXES): the sim's surface here is the streamed 10 m California inset (8 m posting, validated to 3 cm against the vehicles' reported altitudes); ahead of 1-35's freeze seven consecutive postings rise at 0.70-0.95 over 55 m on sand (mean 0.858) - a SUSTAINED face no leader in any run surmounted (max 55 m window surmounted 0.714; the control line over the same ridge 0.19-0.22, 55 m max 0.661); the leader sits at the toe, where a limit near 0.75 (not the raw 0.94) bites; a HMMWV with a higher limit stops 61 m further up the same face. STP's straight legs cross ridge faces the tanks cannot climb, our authoring sampled only the four vertices, and the vendor tests no progress. Not excluded: the formation's mutual speed control pinning the leader - the confirming test is a run that releases 1-35 by a route around the face or by Prefer Roads. DEMO IMPLICATION regardless of the final cause (user decision owed): the interface needs its OWN progress watchdog - no net movement for N sim-seconds while the task reports running -> report to STP and/or re-task - the sibling of the arrival-evidence rule (C15); and route authoring should test max-slope x soil factor along the leg, not vertex heights alone (row 18's validator extended to the leg). Decision owed (user): whether option (a) stays in the demo at all given these numbers; where the terrain copy + area live if it does (vendor default userData\navData, under C:\MAK) |
| 10b | Type mapping = the fidelity table | D | appsettings.Demo.json sets TypeMappingMode=FidelityTable + the 5.2 map. Found on COA-STP1 run 1 (2026-09-06): the base default RealTemplates is the 5.0.2 parity dispatch (every company = Tank Company (USA), hostile included); the table is read only in FidelityTable mode. Its live gate = COA-STP1 run 2 |
| 11 | Type-map coverage of the DEMO ORBAT | V | The type-map LIVE gate passed on the small fixture 2026-09-06 (run L3 201623Z: FidelityTable mode, ~PXY proxies surfaced, 3/3 completion incl. a composed tank company and a BN CP proxy); at scale S4 created all 128 (scratch no-lifeform map). For the demo's real init: `--parse-init <init> <clientId>` + `--typemap-selftest`; lifeform rows need row 17 first; PROXY rows (BN -> HQ-section CP) are a fidelity decision to state in the demo notes |
| 12 | Licence | O (user) | node-locked DEMO licence lapses 2026-09-15 (MAK Sales). Hard prerequisite for any demo after that date |
| 13 | Demo runbook | O | docs/DEMO_RUNBOOK.md: prerequisites, start order (rtiexec -> VR-Forces with GUI -> interface -> STP init -> orders), what the audience sees, the three traps (PATH, RTI dialog once per reboot, leftover instances), stop order DRAFTED 2026-09-14 ~13:20Z: docs/DEMO_RUNBOOK.md rewritten for the operator (Way A one command via scripts/RunScenario.sh, Way B STP-driven; READY line; push endpoints; reset; clean stop; three common failures; UNVERIFIED items listed - the --gui wrapper path and the STP-driven sequence end to end are unrehearsed). |
| 14 | Clean stop | V | the interface's Stop path resigns (VrfC2SimService.StopAsync); VR-Forces stays up; the harness's teardown problems (memory: "TEARDOWN IS NOT SOLVED") are harness problems - verify the interface alone stops cleanly and can be restarted against the running sim |
| 15 | Observability for the operator | O | the app log at INFO is the operator's view (run P: endpoints, bridge start, "Backend discovered", "Connected to C2SIM ... clientId", "C2SIM Initialization", mode line, compose lines, "Init dispatched: N units", PLACEMENT per unit, then per-task CreateRoute/MoveAlongRoute and "VRF task complete"). Owed: one explicit "READY - joined, N backend(s), type map <mode>, waiting for the initialization" line after the join, and a per-order summary ("order <id>: N tasks for M taskees"); the console channel stays -1 REPORTING ASSESSMENT 2026-09-14 (REPORTING_ASSESSMENT_2026-09-14.md sec 5 G9): the READY line and the per-order summary are DONE in the shipped build (G6 log); still missing on the reporting side - the R1 skip counter never names the unit and conflates two failures (hid gap G1 for five runs), no cumulative sent/failed counter (129 silent losses in G6), the loudest log line is a harmless SDK deserialize error (2 per run, B5). B2/B3/B5 in flight on feat/reporting. |
| 16 | Real tasks | later | after this milestone: the 17 verbs of the STP vocabulary (VerbMapping: recognized, Layer-2 not wired). Seen 2026-09-07 (run 103246Z): the ADA battery's DEFEND task ("provide air defence coverage", no location) maps to HoldObjective and is refused with "NO LOCATION GIVEN - CAN'T EXECUTE", which abandons its three successors; a coverage verb with no location needs a non-move intent (hold in place / automatic air defence, UG52 35.2.9). MOVEMENT INSIDE THE REAL TASKS IS THE VENDOR'S (2026-09-07, user: "How is this going to work when we get to real tasks?"): Movement to Contact / Seize Objective / Attack by Fire etc. are vendor unit tasks whose controllers move the unit (UG52 33.7.6 p677, 33.7.7 p678); the interface maps a verb to ONE vendor task and never re-plans routes itself (the per-vertex task split was withdrawn, PREREG_ASSEMBLY_LAYOUT 3g). Terrain-aware movement comes from navigation data, which acts on every planned leg regardless of the task (UG52 30.28 p604; Migration Guide 2.4 p18) - row 10a LANE OPENED 2026-09-14 12:50Z (plan L8): docs-first vocabulary assessment running; Jira epic in STP. ASSESSMENT DONE 13:15Z (TASK_VOCABULARY_ASSESSMENT_2026-09-14.md): the vendor's scripted-task channel is wired in the facade with zero callers; 5.2 tactical tasks are all Lua scripts (many shipped in EntityLevel: company_seize, co_clear, company_breach, plt_attack_by_fire, ...); the init's 35 objective areas already exist in VR-Forces by C2SIM uuid; the constraint is PARAMETER SUPPLY (9/42 tasks have no location; 42/42 self-target); build list V1-V11; six user rulings R1-R6 owed. USER RULINGS Q1-Q7 IN 2026-09-14 20:00Z (docs-first pass complete): Q1 a superseded task -> TASKABRT and its successors are abandoned; Q2 Duration is measured on the sim clock; Q3 wire ambiguity accepted; Q4 no Duration and no geometry = MALFORMED, refused with an error plus TASKABRT (no invented hold); Q5 a paused scenario holds task time while the back end reports; Q6 no interim stop-gap; Q7 rollback is forward-only. Fix pass 2 LANDED on feat/tasking-rulings @ 8db033e (A1 chain gate: COA-STP1 42 dispatches / 0 skips offline, deterministic; A2, B1, B4, B6, B7; Q4 and Q5 built; 18 suites, rulings suite 112 checks); pass-3 cold-start review RUNNING before merge; follow-up STP-809 (the facade must expose back-end status Paused/Playing/gone so a dead back end does not freeze task time under Q5). |
| 18 | Slot validator for the assembly-area layout (C14) | O | ADDED 2026-09-07 (user: "since we are doing the scattering, placing units on unfit terrain is on us ... add building the slot validator to the list"). Before a ring slot is used: heights on a 50 m star around it from the terrain-profile request (already sent for placement) -> local grade vs the unit's vehicles' max-slope (entity parameter; UG52 23.5 "vehicles can just barely move up the max-slope"); soilType from the same reply (the facade drops it today - forward it) vs the water/muck classes of UG52 Table 26; a failing slot is skipped for the next ring slot, with the reason logged. Cannot see feature obstacles (no remote query exists); backstop = a unit whose members have not moved N s after tasking is re-slotted. Evidence: PREREG_ASSEMBLY_LAYOUT 3d/3e (stalls happen on the cross-country approach from the slot as much as under it - the validator should also test the first km toward the route's first vertex). Offline self-test + one run |
| 19 | Progress watchdog (stall detection) - APPROVED 2026-09-13 ("2 as recommended") | O | The vendor never reports a unit that stops making progress: singleTaskControllerComponent.h:192-205 "always returns false", the move-to Lua has no progress test, the decideToGiveUpTask SAMPLE hands the test to the integrator (FINDING_EARLY_STOPS sec 6a). BUILD, report-only: a tasked unit whose members show no net movement for N sim-seconds (N = 120) while its task reports running -> a C2SIM TaskStatus report to STP with TASKABRT (the schema's codes: TASKPEND/TASKSTRT/TASKINPRG/TASKCMPLT/TASKABRT; no automatic re-task); the vehicle position/velocity we already reflect carries it; sibling of C15. Open with the user: whether TASKABRT is the code STP should see, or a distinct rendering on their side. Offline self-test + one run against the known 1-35 freeze (must fire by sim ~500) and the known-good units (must NOT fire) USER RULING 2026-09-14 ~12:40Z ("Ok on 2-3"): TASKABRT IS the TaskStatus code STP sees for a stalled unit - decided; the abort-then-complete ruling (a later TASKCMPLT is never suppressed) stands unless the user overrides. REPORTING ASSESSMENT 2026-09-14: G6 emitted 0 TASKABRT and only TASKCMPLT in 9 h; the watchdog (default OFF) is the only path that would have reported the frozen units. Code decided TASKABRT (user). Merge after pass 4. |
| 20 | Route pre-flight and its DELIVERY (user 2026-09-13: "the interface-side idea is very valuable ... users don't read logs") | O (design) | At order receipt, before dispatch, walk each leg against the SAME public elevation and land-cover tiles the sim streams (validated to 3 cm; elev_face.py / lc2.py in the scratchpad are the prototypes) and the taskee's vehicle limits from the vendor's entity files; flag SUSTAINED faces (the measured discriminator: ~55 m windows above ~0.71 stop an M1A2 on sand; short pitches to 0.9 pass). DELIVERY, not logs: (1) STP task status - TASKABRT for a leg the vehicles cannot traverse, at order time (standard messages, no STP change); (2) STP map - an ObservationReport at the face's coordinates with the numbers, rendered by STP (their end; the channel to invest in); (3) the VR-Forces GUI - the segment drawn on the shared overlay via the remote controller's overlay-object API, for the audience. ORDER OF DELIVERY: map graphic and overlay as WARNINGS first; TASKABRT only after calibration against the legs where units DID pass in P11/G2/G3/G5 shows zero false alarms. Also fixes the +10 m systematic in the route-authoring heights (FINDING sec 1 note) while there. STATUS 2026-09-13 23:25Z: BUILT and COMMITTED 80831b9 (tools/preflight/leg_check.py + README + calibration record; the review's fixes applied and the gates reproduced by the supervisor in a clean checkout: selftest PASS, calibrate --verify-run 3/3 caught, 0/6 false alarms, margin 0.097) + docs/experiments/PREFLIGHT_CALIBRATION_2026-09-13.md. CALIBRATION as first written (2/2 flagged, 0/7 false alarms, margin 0.066 at a 55 m window, threshold 0.90) was REFUTED by its cold-start review (~23:15Z): the labels came from net displacement and missed that 4-27/2/1_A drove 22.7 km and then FROZE for 2,870 s, INSIDE the tool's own worst window (measured 22,697 m vs the window centre 22,702 m) - the highest "passing" ratio was a frozen unit, the margin was an artefact (0.025 corrected) and 0.90 missed it. CORRECTED (labels derived from the traces as closest approach to the next route vertex - units drive THROUGH vertex 1, so an end-position rule mislabels five movers; APPLIED): at a 40 m window the three frozen first legs score 1.098 / 0.990 / 0.966 vs the best clean pass 0.870 -> threshold 0.92, margin 0.096, 3/3 caught, 0/6 false alarms; at 80 m the separation dies (the window is an operating point). ALSO FOUND: a SECOND stop mechanism is active - 1-6 (P11) stopped on descending ground 480 m short of the flagged face and 856/HHC on flat ground; the pre-flight explains SOME stops, not the class (FINDING sec 7b). Wording must stay predictive ("PREDICTED IMPASSABLE"), never "this is why your unit will stop". The P11 probe type map goes into data/ for reproducibility. All tasks: 57 legs, 48 scored (9 sub-metre legs skipped), 14 flagged on 8 units in two clusters (the measured ridge; the southern approach to PL BLUE at ~34.33 N, incl. the ADA platoon at 1.24 because HMMWV-Avenger's max-slope is 0.60). Vehicle limits RESOLVED from the type map + vendor .entity files (parentFile inheritance; the P11 lifeform substitution reproduced). Found and fixed on the way: the land-cover tilesets serve only to L12 (154/165) and L10 (188) - an L13 request silently fell through to a coarser layer. Stated residual risk: one order, one terrain, three positives, 0.097 margin; the class->soil hop is ASSUMED for 7 of the 14 flagged legs (the flagged set is identical under paved-road 1.00 and sand 0.80, so the hop adds or removes nothing here) -> WARNINGS ONLY, as ruled. NEXT: wire it into the interface at order receipt (today a standalone tool with --c2sim-observations / --vrf-overlay outputs); 4-27's third positive CONFIRMED for its LEADER by the console read (FINDING sec 7c: same signature as 1-35; three followers crawled past the stopped leader at 0.30 m/s - the unit did not freeze, the leader did) BUILD ITEM B4 (REPORTING_ASSESSMENT_2026-09-14.md): port the scorer into the interface at order receipt (the product contains no Python, row 6); the emitter shape is already written in leg_check.py:1037-1121 (LocationObservation + NameObservation per flagged leg); STP's rendering of a LocationObservation is UNKNOWN - question 4 of DRAFT_STP_QUESTIONS_2026-09-14.md. FALSE-ALARM CANDIDATE 2026-09-14 (FINDING sec 7d): in G2 three re-formed members of 1-6 crossed the flagged T15 window 20-22 m off its centre on offset lines (0.41-0.43 vs the straight-line 0.744) and drove on; G3's leader froze 28 m from the same point. Offset-line scoring (the formation's lateral spread) is a registered follow-up for the tool. OFFSET-LINE RULING 2026-09-14 (READ_4-27_G3_AND_OFFSET_SCORING): keep the route line as the verdict (only rule with a positive margin under both readings); print the -50..+50 m slot-line ratios as a sensitivity range; the G2 crossings are not a false alarm (re-formed unit at 0.02x); the real false alarm is 4-27/T5 in G3 (leader's line 0.941, crossed) - one leg, logged, threshold unchanged; wording 'some of your vehicles'. |
| 21 | Customizations SURVIVE REINSTALLS of C:\MAK (user 2026-09-13: "we need something that survives reinstalls") | O (design, docs read 2026-09-13) | Vendor mechanisms per kind: (a) platform files (.ope, the navigation-profile binding) and movement definitions (.sysdef, slope-avoidance-factor) -> an INCLUDING custom SMS that holds only the changed files (Migration Guide 1.2.2 p12 "Custom SMSs that include and extend a VR-Forces SMS are much easier to upgrade"; UG52 68.3.1 p1310 higher-priority SMS overrides; 68.7.1 p1323), kept in our repo; (b) generated navigation data -> the generator's --navDataDir / --outputPath (observed) and the sim's --userDataDir (UG52 Table 11 p184) - so the data lives at a short root WE own outside C:\MAK, referenced by the terrain copy; (c) terrain copies (.mtf) -> loaded by absolute path (UG52 -T p184; our fixture already does this); (d) scenarios -> absolute path (-L, UG52 p182; the launcher passes a relative userData path today - one-line change); (e) navigationProfiles.mtl and vrfSim.mtl -> NO dedicated redirect; the vendor's own prescribed method is copy-forward of ./appData, ./data, ./userData (UG52 3.7.1 p126), so a changed profile ships in our repo with a deploy step that copies it into place with backup + diff; --appDataDir as a no-touch alternative is UNCONFIRMED for this file (a one-launch test if ever needed). DECISION TAKEN (follows from the user's requirement): the navigation data's permanent home is a short root outside C:\MAK (C:\C2SIM\vrf-nav\), NOT the vendor's userData\navData (whether a reinstall preserves userData is not stated). Nothing under C:\MAK but the deployed fixture and, only if G6 fails and the user sanctions it, one settings file STATUS 2026-09-14 00:01Z: the nav-data home is REALIZED - C:\C2SIM\vrf-nav\navData\MAK Earth (online)\ holds the full-COA area (26,525 files, 1.2 GB, generated 3.3 h headless), its runtime config points there, the terrain COPY in tools/navdata/out registers it, and C:\MAK holds 0 nav entries; the .mtf copy and the deployed fixture are the only things that need re-creating after a reinstall (both scripted: make_nav_terrain.py + build_fixture.py). USER RULING 2026-09-14 ~12:40Z ("Ok on 2-3"): if the custom including SMS cannot live outside C:\MAK, its placement under C:\MAK is SANCTIONED; relocating appData via --appDataDir (UG52 Table 11 p178) for the G8 gamewareMemorySize variable is SANCTIONED if G8 is needed. CUSTOM SMS HOME REALIZED 2026-09-14 ~13:00Z: C:\C2SIM\vrf-sms\ (sibling of vrf-nav) holds C2SIM_EntityLevel_AbstractGraphs.sms - includes the shipped EntityLevel.sms unchanged and overrides ONE script by script id (UG52 68.3.3 p1312 priority, 68.3.4 p1313 'scripts in the highest priority SMS supersede', absolute paths UG52 Table 15 p271 / 12.2.1 p352); fixture R9_Mojave_Empty_52_NavAO_AG references it by absolute path (FixtureGen --sms, commit 5131f17). Run-time proof owed: the override's printInfo line in a member console. G8 APPDATA RELOCATION BUILT, committed on main a1f3b7d 2026-09-14: C:\C2SIM\vrf-appdata\appData byte copy with cache/data/userData junctions, one change loadAllNavigationDataOnTerrainLoad 1; LaunchVrf52 -AppDataDir; record docs/experiments/APPDATA_RELOCATION_2026-09-14.md; runner passthrough -VrfAppDataDir / --vrf-appdata-dir being applied now; validation launch owed (caveat: --appDataDir shares the option-parsing path with --logFileName, the 1-in-3 startup crash - if the launch dies at startup, drop the option once first). G7 ATTEMPT 4 MEASURED 2026-09-14 15:42Z (run 20260914T154243Z, 009c674): gates 36/36; legs 1 (1 seam) and 2 (4 seams, up to 1,994 m) mesh-planned 4/4; leg 3 (10 seams, 4,914-5,048 m) refused 4/4 once each, feature planner drove it to arrival. Reading B FALSIFIED (connected across seams); ceiling (1,994, 4,914] m is AREA-dependent (AO20 planned 10.1 km) = reading A -> G7b abstract graphs (fixture _AG; order adds a 4th task re-issuing the long leg after arrival, CR2) then G8 budget/memory via the relocated appData. Timing is not a discriminator. LAZY LOAD CORRECTED: async ~237 s / 2.3 GB stream from PLACEMENT; the 240 s settle covered it with 9 s margin; appData load-with-terrain validation next (appdata_val launch line ready: stock fixture, --vrf-appdata-dir, AtOrder, no settle, 300 s). G6/G7 CLOSED 17:15Z: the mesh refusal on the 41x54 km area was the vendor script's hard-coded useAbstractGraphs=false (ground-vehicle-move-to.lua:488). G7b (run 20260914T170824Z, custom including SMS C:\C2SIM\vrf-sms\C2SIM_EntityLevel_AbstractGraphs.sms) planned the 4,989 m / 10-seam leg 8/8 (28-35 abstract points) with 0 refusals; the same leg was refused 4/4 (attempt 4, stock SMS), 8/8 with gamewareMemorySize 128 (G8, run 165919Z) and again with gamewareQueryTimeBudget 50 ms (G8b, run 172134Z). Memory and time budget are NOT the lever. The custom SMS is product configuration (FixtureGen --sms default; STP-804). appData validation (run 164906Z): with loadAllNavigationDataOnTerrainLoad 1 the first goals pass both gates under AtOrder with ZERO settle (attempt 3 failed 18/18); the placement-triggered 2.3 GB stream persists and is cache-bound (237 s cold, 15-20 s hot) -> warm the cache in the prepare step. OPEN: whether abstract-graph paths avoid the ridge faces (FINDING_EARLY_STOPS) - 1-35 lane test with the custom SMS owed. Full harvest G7B_G8_RESULTS_2026-09-14.md in progress. CORRECTION 2026-09-14 (harvest G7B_G8_RESULTS_2026-09-14 sec 1.5/3/3.1, records executor): the appData validation sentence above is WRONG and is WITHDRAWN, not deleted - run 164906Z had 8 current-point gate failures (`fail in action Is current point in nav area?`), and every tank's slot move and first leg ran on the FEATURE planner because the order reached the bus 4.7-7.7 s before the area row; loadAllNavigationDataOnTerrainLoad 1 is a NULL RESULT (the 2.3 GB stream is placement-triggered at 1 and at 0 alike) although the relocated tree WAS read (19 settings files rewritten per launch, one-to-one with --appDataDir). Unusable window = first entity placement -> first `New Primary nav area` row from a placed platform: 9.1-12.1 s warm, 237 s cold. DEMO-FLOW RULE: gate PushOrder on that row (runner item, owed), warm the cache in the prepare step, CreationPolicy=AtInit so members are placed before the wait; a fixed settle if used: >= 30 s warm, 240 s+ cold. G7c-gate 2026-09-14 19:08Z (run 20260914T190751Z, -PreOrderGate NavArea, same tree and policy as run A): SINGLE-VARIABLE confirmation of the custom SMS as the fix for the long-leg refusal - 0 gate failures, 32 gate successes, 32 mesh plans, 8/8 long legs planned (28-35 pts outbound, 33-35 pts return), 0 refusals; CR5 (the settle/AtInit confound) and run C's appData confound are both removed, the only difference from refusing run A is the SMS override. Ready gate FIRST LIVE USE fired at +118.1 s on a COLD file cache (the `New Primary nav area` row); a companion run G7c (20260914T185945Z, same fixture, no gate) found the cache cold again within an hour of build/review load and was VOID for the SMS question (nav area never registered, 31/31 goals gate-failed). Demo-flow implication: the cache goes cold within about an hour under build load - the prepare step must warm it immediately before the demo and the READY GATE, not a fixed timer, is the control. DOCS PASS 2026-09-14 20:00Z (NAVDOCS_ABSTRACT_GRAPHS_AND_SLOPE_2026-09-14.md, being committed by another executor): the flat A* query has a documented 200 m propagation box (Autodesk AStarQuery option; per-platform propagation-box-extent in Ground_Vehicle.ope) - the successful abstract routes deviated 231-292 m, outside it; abstract graphs are documented COST-BLIND; the ridge face is legal to the mesh (slope-max 46 deg, cost no-go unreachable at factor 1.0, soil invisible), so no planner flag routes around it - the documented lever is slope-avoidance-factor. PLAN CHANGE: the ridge test is replaced by N1 (propagation-box-extent 2000, stock Lua, the G7 route; PREREG_N1_N2_CORRIDOR_SLOPE_2026-09-14.md being written) then N2 (+ slope-avoidance-factor 2.0 on the 1-35 lane); N3 (the abstract-graph SMS on the ridge) only if N1 refuses. FixtureGen's abstract-graph-SMS default stays PROVISIONAL pending N1 (STP-804 comment). N2b 21:05Z: the abstract-graph override is NOT sufficient by itself - members created AtOrder 22 s after the init's own 1-35 proxy was placed in the same cell got 0 points for every goal 0.3-5 s after placement, 5-10 s after the run's first area row (5th ridge freeze); if N2c (300 s task delay) plans, the READY GATE on the first area row is insufficient on its own and the demo flow needs a per-taskee readiness or a settle after materialisation (STP-806). |
| 22 | Runner robustness: a run must never leave the sim and the interface running (found 2026-09-14: the G6 runner pwsh was KILLED from outside at t+127 s of its window - TerminateProcess(-1) signature, every crash path refuted by measurement, killer unidentified; its in-process finally could not run; sim + interface ran 9 h, 5.9 GB log) | B (built + committed f6e68d9 2026-09-14 ~12:30Z; verified by the supervisor: parse, 64-bit gate fires under the 32-bit host, dry run exit 0 under the 64-bit host; FIRST LIVE USE = G7) | docs/experiments/RUNNER_EXIT127_2026-09-14.md. Fixes in flight: (1) 64-bit gate - bare `pwsh` resolves to the 32-bit 7.6.1 on this machine's PATH, which undid the 2026-09-07 mitigation silently; (2) teardown marker runner.teardown-ran + an OUT-OF-PROCESS wrapper backstop (scripts/RunScenario.sh: stdout to a FILE, no tee/pipe - a child inheriting the pipe held the supervisor's harness open for nine hours; StopIface + StopVrf52 + observers.stop if the marker is absent); (3) the status line tail-read 256 KB and printed '(no samples)' on a 1 GB trace - replaced by a backward block scan; (4) the loop re-read the whole app log every 5 s - incremental reader. Deferred: a detached watchdog for the case the wrapper dies too. Rule: no Stop-Process sweeps during a run window; cleanup excludes the runner pid. A2 RUNNER WATCHDOG + PRE-ORDER SETTLE DONE, committed on main 374ea49: scripts/RunnerWatchdog.ps1 detached process; -PreOrderSettleSecs / --pre-order-settle; explicit -WatchSecs does not grow with the settle - the runner warns; tested offline a/b/c; NOT yet exercised inside a live run. FIRST LIVE USE of the watchdog clean (armed Stage 3w, 750 ms survival, two-observation confirm, stood down on teardown-ran; wrapper used the run-dir pointer). DEFECT: -StopWhenComplete never fires - condition (4) needs an RPT trace line that no run has ever produced (0 in 5 traces); fix = accept the taskee's post-TASKCMPLT C2SIM position report (STP-794). Licence: c8730e7 resolves MAKLMGRD_LICENSE_FILE User-then-Machine per process; expired-licence pre-launch check. Probe runs 4-7 of the day all tore down cleanly with the watchdog standing down each time; runner fixes in progress (STopWhenComplete on C2SIM position evidence, sampler sized from the derived window, relocated connection-config path, clock labels, licence resolver in the 5.0.2 launcher). READY GATE -PreOrderGate NavArea (7531bc6) fired live at +118 s; the stop rule closed windows early in two runs (the C2SIM-capture satisfier is live via ListenReports incremental capture, 0999eeb); eleven clean teardowns today. |
| 23 | REPORTING gaps (assessment 2026-09-14; user: "Start looking into reporting in parallel") | O (B5/B3/B1/B2/B8 in flight) | docs/experiments/REPORTING_ASSESSMENT_2026-09-14.md. G6 on the bus: 14,698 bodies (102 NameObservation + 14,596 Position + 0 TaskStatus in the 1,200 s capture; 8 TASKCMPLT in 9 h, ALL from arrival evidence C15 - the 3 vendor completions were swallowed; 34 of 42 tasks never got a status; the first status came 1,659 s after the order; 129 pushes failed silently; 1 of 128 units (the platform-mapped brigade 2/1_AD/25_) never reports a position because VR-Forces truncates its marking to 10 chars and our lookups are by name). Ranked gaps G1-G11 and build items B1-B10 in the record. Decided: TASKABRT for stalls (user), TASKABRT for refused/skipped tasks (supervisor, user may override). Queued: B4 pre-flight port, B7 heading+speed (native), B9 bundle run, B10 five questions to STP (DRAFT_STP_QUESTIONS_2026-09-14.md - the user sends). JIRA: epic STP-777, items STP-778..787. STP-SIDE FACT 13:10Z (STP_PARSE_CHECK_2026-09-14.md): our bodies parse on every SDK STP pins, but STP's C2SimBridge ParseReportContent consumes ONLY PositionReportContent - TaskStatus and ObservationReport are parsed and DISCARDED; bundle shape A (ours) is fine, shape B silently loses all but the first. So TASKSTRT/TASKABRT and the pre-flight observations need an STP-side change to be seen (Jira on the STP side). |
| 17 | DI-Guy character data (makData19 DI-Guy package, 6.33 GB) | O (user) | Found 2026-09-06 (PREREG_COASTP1_52_RUN1 sec 6-7): without `C:\MAK\SharedData\19\latest\ModelData\Lifeforms\DIGuy` the 5.2d headless sim crashes (0xC0000005, DtDiGuyController::determineInitialHandItem) at the first DI-Guy human it creates - i.e. at the first infantry / mortar / CSS / fire-support unit the fidelity table maps. A demo whose ORBAT has any infantry needs the package installed first; the interim (vehicle-only proxies for lifeform rows) is a fidelity regression = user ruling |

## What this milestone does NOT include
GUI automation of any kind; the harness features (ledger, observers, scoring, StopWhenComplete);
golden-parity to 5.0.2 (ARCHIVE).

## Exit criterion
An operator following DEMO_RUNBOOK.md, with no repo tooling beyond the three start scripts,
brings up rtiexec + VR-Forces (GUI) + the interface, pushes the R9 init and the R9 order from
STP (or PushInit/PushOrder as the STP stand-in), and sees the three units created and moving in
the GUI and their position + task-status reports in STP - twice in a row after a reset,
without restarting VR-Forces.
