# PLAN - parallel lanes to the demo (2026-09-14, supervisor: Fable 5.1)

User, 2026-09-14 ~11:00Z: "Anything you can do in parallel? These nightlong unattended waits are not
helping us deliver this" / "Start looking into reporting in parallel" / "Make a plan".
Context: G6 STOPPED on its instrument gate (PREREG_NAVDATA_G6 sec 5); the runner was killed from outside
and left the sim up for nine hours (RUNNER_EXIT127); the DEMO licence lapses 2026-09-15 (user holds the
renewal, download owed). Gate for this document: PLAN (a written scope; the user corrects it here).

## 0. Rules that shape the plan
- The ONLY serial resource is a TIMED RUN (~25 min of an idle machine). Everything else runs in parallel:
  executors in worktrees, analyses on captures, docs reads, builds on branches.
- A timed run is polled from the FOREGROUND with bounded waits; the supervisor never ends its turn while a
  run is open (lesson 2026-09-14). The runner's teardown is out-of-process (lane L4) before the next run.
- One variable per preregistered run; every claim of cause carries its falsifier; the CLOSED lists in
  DESIGN_ORBAT_TO_VRF and the FINDING remain canonical.
- Model tiering: Sonnet extracts (docs), Opus executes/verifies/reviews, the supervisor decides.

## 1. Lanes (all live now unless marked)
| # | Lane | Objective / deliverable | State 2026-09-14 11:30Z | Next step | Gate | User? |
|---|---|---|---|---|---|---|
| L1 | Mesh query refusal | Why vrf:findPathToLocation returns 0 points for every multi-km goal inside a loaded 41x54 km area (G6 sec 5). Deliver PREREG G7 with ONE variable. | Docs read DONE (NAVMESH_QUERY_DOCS): the query's documented failure predicates are outOfWorkingMemory / hadComputationError (class_dt_nav_area_query), gamewareMemorySize 16 MB is the working-memory cap "for larger nav areas" (vrfSim.mtl:383, UG52 p1670), the shipped ground-vehicle-move-to.lua calls the query with useAbstractGraphs = FALSE (:488) although the API says true "can speed up long path planning queries" and the area carries 8,810 abstract-graph files. Goal-distance pairing across G1/G2/G3/G6 RUNNING. N2b RUN 21:05Z (205046Z): P20a MISS -> STOP - the AG query returned 0 points for EVERY goal of the members created AtOrder at the destack start 22 s after the init's 1-35 proxy was placed in that same cell (slot move at +0.3 s and the 6.6 km leg at +5 s; G7c-gate's cell had been resident 119 s), 5th ridge freeze at the P11 point (0.7 m); abstract coverage of the leg REFUTED offline as the cause (generation log: all leg sectors carry an abstract graph); load-timing (T) vs mesh-hole (L) undecided -> N2c RUN 22:25Z (215944Z): REFUSED again at +304 s -> load timing FALSIFIED; the refusal is SPATIAL (the six start positions at the destack start; 6th freeze at the P11 point, 1.4 m) - next: print the query failure reason from our override Lua, read the nav profile, then N2d - RUN 22:50Z (224505Z, init copy places 1-35 500 m along the leg): short slot moves PLAN 4/6 at the new spot, EVERY long goal refused (2.3-20 km; regional, not distance) - and the unit drove the whole COA route V0->V1->V2->V3 on straight paths 1.3 km NORTH of the freeze line (first completion of 1-35's route in any run; the freeze is a LINE property -> pre-flight + north-shift is the demo remedy, STP-804/806) - PREREG_N1_N2 sec 10.3. | ATTEMPTS 1-3 VOID (blank line killed the STOMP pump; a lone platform never enters the Lua planner; under AtOrder the
members are tasked ~175 s before the lazily-loaded area is recognised - a FINDING for the demo flow). ATTEMPT 4 =
CreationPolicy AtInit + pre-order settle >= 240 s (runner -PreOrderSettleSecs being added); appData relocation with
loadAllNavigationDataOnTerrainLoad 1 being prepared (sanctioned). PAIRING DONE 11:50Z: AREA-dependent (10.1 km planned on the 1,600-sector area, 0 points on the 8,856-sector
area for the same bytes; fast refusals) -> G7 = the CROSS-SECTOR PROBE (one unit, 0.6 / 2 / 5 km legs on MojaveCOA,
consoles 4; order being authored) decides refusal-vs-disconnected-data; G7b = the abstract-graph SMS only if the
data is connected and 5 km fails; (b) DONE 13:00Z: custom including SMS at C:\C2SIM\vrf-sms (5131f17), fixture _AG deployed; design of the CUSTOM INCLUDING SMS (UG52 68.3.1 / Migration 1.2.2) that overrides only ground-vehicle-move-to.lua with useAbstractGraphs = true, at a repo-owned root if the SMS reference accepts an absolute path (survives reinstalls; else user sanction for the location); (c) PREREG G7: variable = useAbstractGraphs; falsifier = still 0 points -> G8 variable = gamewareMemorySize via --appDataDir (UG52 Table 11 p178, the reinstall-safe mechanism). ATTEMPTS 1-3 VOID (recorded in PREREG sec 5); design v3 copied to docs/experiments/G7_PROBE_DESIGN_v3_2026-09-14.md (cc2485d); ATTEMPT 4 launch line ready and dry-run clean (AtInit + --pre-order-settle 240 + --watch-secs 1800, unchanged deployed build); waits for an idle machine (no agents during a timed run). G8 appData relocation BUILT, committed on main a1f3b7d (C:\C2SIM\vrf-appdata\appData byte copy with cache/data/userData junctions, one change loadAllNavigationDataOnTerrainLoad 1; LaunchVrf52 -AppDataDir; record docs/experiments/APPDATA_RELOCATION_2026-09-14.md); runner passthrough -VrfAppDataDir / --vrf-appdata-dir being applied now; validation launch owed (caveat: --appDataDir shares the option-parsing path with --logFileName, the 1-in-3 startup crash - if the launch dies at startup, drop the option once first). G7 ATTEMPT 4 MEASURED 2026-09-14 15:42Z (run 20260914T154243Z, 009c674): gates 36/36; legs 1 (1 seam) and 2 (4 seams, up to 1,994 m) mesh-planned 4/4; leg 3 (10 seams, 4,914-5,048 m) refused 4/4 once each, feature planner drove it to arrival. Reading B FALSIFIED (connected across seams); ceiling (1,994, 4,914] m is AREA-dependent (AO20 planned 10.1 km) = reading A -> G7b abstract graphs (fixture _AG; order adds a 4th task re-issuing the long leg after arrival, CR2) then G8 budget/memory via the relocated appData. Timing is not a discriminator. LAZY LOAD CORRECTED: async ~237 s / 2.3 GB stream from PLACEMENT; the 240 s settle covered it with 9 s margin; appData load-with-terrain validation next (appdata_val launch line ready: stock fixture, --vrf-appdata-dir, AtOrder, no settle, 300 s). G6/G7 CLOSED 17:15Z: the mesh refusal on the 41x54 km area was the vendor script's hard-coded useAbstractGraphs=false (ground-vehicle-move-to.lua:488). G7b (run 20260914T170824Z, custom including SMS C:\C2SIM\vrf-sms\C2SIM_EntityLevel_AbstractGraphs.sms) planned the 4,989 m / 10-seam leg 8/8 (28-35 abstract points) with 0 refusals; the same leg was refused 4/4 (attempt 4, stock SMS), 8/8 with gamewareMemorySize 128 (G8, run 165919Z) and again with gamewareQueryTimeBudget 50 ms (G8b, run 172134Z). Memory and time budget are NOT the lever. The custom SMS is product configuration (FixtureGen --sms default; STP-804). appData validation (run 164906Z), CORRECTED 2026-09-14 (harvest G7B_G8_RESULTS_2026-09-14 sec 1.5/3/3.1): the "gates pass at once" prediction MISSED - 8 current-point gate failures, logged as `fail in action Is current point in nav area?` (the order reached the bus 4.7-7.7 s before the area row); loadAllNavigationDataOnTerrainLoad 1 is a NULL RESULT (the 2.3 GB stream is placement-triggered at 1 and at 0 alike) although the relocated tree WAS read (19 settings files rewritten per launch, one-to-one with --appDataDir). The unusable window is first entity placement -> first `New Primary nav area` row from a placed platform: 9.1-12.1 s warm, 237 s cold. DEMO-FLOW RULE: gate PushOrder on that row (runner item, owed), warm the cache in the prepare step, CreationPolicy=AtInit so members are placed before the wait; a fixed settle if used: >= 30 s warm, 240 s+ cold. OPEN: whether abstract-graph paths avoid the ridge faces (FINDING_EARLY_STOPS) - 1-35 lane test with the custom SMS owed. Full harvest G7B_G8_RESULTS_2026-09-14.md in progress. G7c-gate 19:08Z: single-variable confirmation of the abstract-graph cause (8/8 long legs, 0 refusals, same tree and policy as the refusing run); ready gate first live use fired at +118 s (cold); stop rule first live use closed the window early; ridge test PAUSED pending the Gameware Navigation docs pass (abstract graphs, mesh slope/soil). DOCS PASS 20:00Z after the user's "drift back to probing" call (record NAVDOCS_ABSTRACT_GRAPHS_AND_SLOPE_2026-09-14.md, being committed by another executor): the flat A* query has a documented 200 m propagation box (Autodesk AStarQuery option; per-platform propagation-box-extent in Ground_Vehicle.ope) - the successful abstract routes deviated 231-292 m, outside it; abstract graphs are documented COST-BLIND; the ridge face is legal to the mesh (slope-max 46 deg, cost no-go unreachable at factor 1.0, soil invisible) so no planner flag routes around it - the documented lever is slope-avoidance-factor. PLAN CHANGE: ridge test replaced by N1 (propagation-box-extent 2000, stock Lua, G7 route; PREREG_N1_N2_CORRIDOR_SLOPE_2026-09-14.md being written) then N2 (+ slope-avoidance-factor 2.0 on the 1-35 lane), N3 (AG ridge) only if N1 refuses; FixtureGen's AG default is PROVISIONAL pending N1 (STP-804 comment). | PREREG | SMS location if under C:\MAK; G8's appData relocation |
| L2 | Reporting | What STP and the audience SEE: position reports, TaskStatus (TASKCMPLT / TASKABRT), ObservationReports (pre-flight warnings, watchdog). Assessment -> ranked build list -> builds. | ASSESSMENT DONE 12:30Z (REPORTING_ASSESSMENT_2026-09-14.md): 11 gaps, 10 build items; the SDK deserialize error is our own root-sniff fallback (cosmetic, B5); a platform-mapped unit never reports (name truncation, B3); only TASKCMPLT is ever sent (B1: TASKSTRT at dispatch, TASKABRT for refused/skipped); silent push loss (B2). BUILD RUNNING on feat/reporting (off 08146a2): B5, B3, B1, B2, B8. DRAFT of the five STP questions written (B10, user sends). REVIEW FIX PASS DONE ~13:30Z, feat/reporting @ eb80864: two latent MAJORs in NameRegistry closed (exact-match-plus-prefix now reported, identity hijack prevented); CodeForCompletion(success, taskContinues); empty push response = failed delivery; cumulative sent/failed counter; fail-first proven 6+5 checks. Owed: native success() wiring (landing in the integration merge), four live gates. | Build the top items on a branch with self-tests; wire pre-flight ObservationReports at order receipt; decide TASKABRT semantics. | STANDARD; RULE for TASKABRT | TASKABRT code decision |
| L3 | Watchdog + sim clock | C16 progress watchdog (report-only, default OFF) with the window on the back end's scenario clock; calibrated 240 wall / 360 sim (RECAL_STALL_SIMSECONDS). | REVIEW-UNTIL-CLEAN DONE 11:50Z: pass 3 (REVIEW3_SIMCLOCK_08146a2) found a +Infinity hole and mode-thrash dormancy; pass-4 fixes f052ea7 (66/66 stall checks, wall path identical to 51d78a5 on ten feeds, Decide byte-identical) verified by the supervisor. MERGE AFTER G7 (keeps G7's binary pinned). | Pass-3 cold review -> merge watchdog + sim-clock into main (StallClock default wall) -> rebuild main -> the next run pins that build (watchdog OFF, so no behaviour change). | review-until-clean (max 4) | none |
| L4 | Runner robustness | A run can never leave the sim + interface running; status line truthful on a 1 GB trace; 64-bit host enforced; no inherited stdout pipe. | DONE 12:30Z (f6e68d9): 64-bit gate, launched/teardown markers + wrapper backstop, block-scan status line, incremental log read, RUNBOOK 0.5.14. A2 detached watchdog deferred; killer unidentified. | First live use = G7 (scripts/RunScenario.sh). | STANDARD | none |
| L5 | Records | This plan; the entry HANDOFF doc; DEMO_READINESS rows 19-22; memory. | Current. | Update the same turn a lane settles. | - | none |
| L6 | User-owned | Licence renewal (row 12, lapses 2026-09-15); DI-Guy data package (row 17, any lifeform run); TASKABRT code; C:\MAK sanctions above. | 12:40Z user "Ok on 2-3": TASKABRT DECIDED as the stalled-unit code; SMS placement under C:\MAK and --appDataDir relocation SANCTIONED if needed. Licence + DI-Guy still open. | - | RULE | yes |
| L8 | Task vocabulary (user goal beyond MOVE) | Every STP verb -> vendor task -> completion evidence; build list. | DOCTRINE READ DONE ~14:10Z + USER RULINGS R1-R4 (record: DOCTRINE_FOR_TASKING_RULINGS_2026-09-14.md + TASK_VOCABULARY_ASSESSMENT_2026-09-14.md sec 7.1); R5/R6 open; foundation V2+V3 BUILT on feat/tasking-foundation (5b58c42, 0995fab: scripted-task variables bool/int/string/location, init lines/points opt-in); next = merge into feat/integration after its review, then V4 (uuid linkage) / V5 / V9 rewritten under the rulings. | 2026-09-14 pm: ALL SIX RULINGS IN (R5 entity-first + user-selectable mode; R6 filter-enforcement run then fan-out); rulings build R1-R4 running on feat/tasking-rulings; next V4b embedded-location interpretation, then the R6c probe run after the integration deploy. 2026-09-14 pm: REVIEW of feat/tasking-rulings 5c67d41 = FIX FIRST (M1-M5; REVIEW_RULINGS_5c67d41_2026-09-14.md); fix pass running (merges feat/integration 26efe0c first); Q1-Q4 asked of the user 2026-09-14, supervisor defaults applied meanwhile. USER RULINGS Q1-Q7 IN 20:00Z: Q1 superseded -> TASKABRT + successors abandoned; Q2 Duration measured on the sim clock; Q3 wire ambiguity accepted; Q4 no Duration + no geometry = MALFORMED, refused with error + TASKABRT, no invented hold; Q5 a paused scenario holds task time while the back end reports; Q6 no interim stop-gap; Q7 rollback forward-only accepted. Fix pass 2 LANDED on feat/tasking-rulings @ 8db033e (A1 chain gate: COA-STP1 42 dispatches / 0 skips offline, deterministic; A2, B1, B4, B6, B7; Q4, Q5 built; 18 suites, rulings suite 112 checks); pass-3 cold-start review RUNNING before merge; follow-up STP-809 (facade exposes back-end status Paused/Playing/gone so a dead back end does not freeze task time under Q5). | Six user rulings R1-R6, then V1-V3 (success flag, ScriptVar, lines/points), then the ATTACK and HoldObjective families. | PLAN V4b BUILT+MERGED 00:50Z (16995b3; DESIGN_V4B_EMBEDDED_LOCATION): embedded Location read by VERB AND SHAPE (Route/ObjectiveArea/Point/None; closure test calibrated on the init's 12 areas; T32 SEIZE is a 23.6 km AXIS, kept a route), MapGraphicID precedence, on-the-fly control area once per task; COA-STP1 census 9 none / 22 point / 11 route / 0 rings (STP-801 linearises); suite 176/0; no movement change. Worktree note: tools/preflight/preflight_cache is gitignored - copy it into a worktree before --preflight-selftest (hit twice tonight; RUNBOOK item owed). | mapping rulings |
| L9 | STP-side parse check (B10 part 1) | Our three report bodies through STP's own parser (SDK 1.3.1 strict; STP-615). | DONE 13:10Z: all three shapes parse on 1.3.0/1.3.1/1.4.0; STP DISCARDS non-position reports (bridge handles only PositionReportContent); bundle shape A ok, B loses data. | STP-side change needed for TaskStatus/Observations; questions 2-4 partly answered. | - | none |
| L10 | Records refresh | HANDOFF_2026-09-14_PARALLEL_LANES.md (cold-start entry, CLOSED tripwires) + DEMO_RUNBOOK.md (row 13, for the operator). | DONE 13:20Z: HANDOFF_2026-09-14_PARALLEL_LANES.md (191 lines, CLOSED tripwires) + DEMO_RUNBOOK.md (Way A / Way B, UNVERIFIED items). | Commit; rehearse the --gui and STP-driven paths. | - | none |
| L11 | Second read of 4-27 on G3 + offset-line pre-flight scoring | Reproducibility of the crawl-past; whether formation-band scoring keeps the calibration (row 20 false-alarm candidate). | DONE 13:25Z: the P11 pattern is a LANE property (3 of 6 stop at the toe in both runs; leader crossed in G3); keep the route line as the verdict, print the slot band as sensitivity. | Tool: print the band; FINDING 7e. | - | none |
| L12 | A2 detached run watchdog (STP-794) | Teardown even when the wrapper dies. | RUNNING 12:50Z (scripts only). DONE, committed on main 374ea49: scripts/RunnerWatchdog.ps1 detached process; -PreOrderSettleSecs / --pre-order-settle; explicit -WatchSecs does not grow with the settle - the runner warns; tested offline a/b/c; NOT yet exercised inside a live run. FIRST LIVE USE of the watchdog clean (armed Stage 3w, 750 ms survival, two-observation confirm, stood down on teardown-ran; wrapper used the run-dir pointer). DEFECT: -StopWhenComplete never fires - condition (4) needs an RPT trace line that no run has ever produced (0 in 5 traces); fix = accept the taskee's post-TASKCMPLT C2SIM position report (STP-794). Licence: c8730e7 resolves MAKLMGRD_LICENSE_FILE User-then-Machine per process; expired-licence pre-launch check. Probe runs 4-7 of the day all tore down cleanly with the watchdog standing down each time; runner fixes in progress (STopWhenComplete on C2SIM position evidence, sampler sized from the derived window, relocated connection-config path, clock labels, licence resolver in the 5.0.2 launcher). READY GATE -PreOrderGate NavArea (7531bc6) fired live at +118 s; the stop rule closed windows early in two runs (C2SIM-capture satisfier live via ListenReports incremental capture 0999eeb); eleven clean teardowns today. | Live use on the next non-timed run. | STANDARD | none |
| L7 | DEMO-READY residue | Rows 13 (runbook), 15 (operator observability), 18 (slot validator). | Open. | After L2's list - 13 and 15 overlap with reporting delivery. | STANDARD | none |
| L13 | Integration | Merge feat/sim-clock + feat/reporting + feat/heading-speed + feat/preflight-port into feat/integration (the real success flag wired into CodeForCompletion), native rebuild + all self-tests. | RUNNING 2026-09-14 pm. feat/integration @ 26efe0c = + tasking foundation (V2/V3) merged, 10 suites green, native rebuilt in the worktree; G-B PASS; G-A (rebuild + deploy to TEN consumers + re-pin) is next after the rulings fix pass merges; then the validation runs. UPDATE 20:00Z: feat/integration @ 26efe0c awaits the rulings merge (after pass 3), then main, then G-A (native rebuild + deploy to TEN consumers + re-pin), then the validation runs (C16 with Vrf__StallDetection=true, reporting live gates, the 42-task COA-STP1 chain at DurationScale 0.05, B7 heading sense). | Cold-start review of the integrated diff; deploy the 7 DLL copies AFTER G7 attempt 4. | STANDARD 22:05Z: MERGED to main (0f4d09e) - rulings 1ffb4ee via integration; fix pass 3 record FIXPASS3_RULINGS_1ffb4ee; G-A DONE 22:45Z (6f91feb + pin 4d5f59c): native bridge rebuilt (member-presence proof: TryGetEntityKinematics/SimTimeSeconds absent before, present after), six 5.2 consumers at one hash, 18/18 suites, rulings 136/0; FINDING: SmokeTest/CreateTaskAgg/ResetVrf/SetSimRate have no BridgeConfig and are 5.0.2-only (user ruling: convert with StackIdentity + live join gate, or declare 5.0.2-only); RUNBOOK sec 9 corrected; V2 (the 42-task chain at DurationScale 0.05) RAN 23:09Z (230706Z): chain depth 840/1080, 31 gate lines, 0 backstop skips, 41 dispatched (T26 skipped after VR-Forces failed 1-1's patrol T25 -> TASKABRT, the ruled cascade), 40 timed TASKCMPLT at Duration x 0.05 of SIM time, T13 after its 600 s delay, 9 in-place, 0 malformed, 0 push failures, 0 tick-phase/MissingMethod; SIM CLOCK stepped back ~2 s twice (RolledBack, harmless) - PREREG_V2_CHAIN_DS005 RESULTS. Runner StopWhenComplete did not fire (its own defect: counts TASKCMPLT only and mis-detected every taskee) - fix/runner-stop-rule dispatched. STP-809 BUILT (c9576a0/70d60b1/f8aebc5; suite 136 -> 148/0; facade BackendControl + ActiveBackendCount; HOLD vs WALL on the CONTROL STATE) and MERGED 00:10Z (4cd84d7) - G-A PIN STALE, rerun owed after V13; runner stop rule FIXED (ad4b191, merged ff15a4e: the end-of-line regex anchor broke when B1 appended a reason; terminal = CMPLT or ABRT per task). B7 (V4) PASS from V2's capture 23:45Z: heading median 0.1 deg off the derived course (degrees, right sense), Speed in m/s of sim motion (the reported/derived ratio tracks the sim ratio 0.48 -> 0.78). V13 (V1+V3 in one run) 00:15Z (001159Z): Q4 refusal at receipt (TASKABRT before any TASKSTRT), TASKSTRT once per task, C16 FIRED on the frozen 1-35 (no member > 50 m in 360 sim s, max 40.4 m; TASKABRT before the 720 s end) and stayed SILENT on 1-1 (TASKCMPLT by time at 740/720 s), 0 push failures - and the FIXED STOP RULE CLOSED THE WINDOW at +184 s of 900 (first live closure). Debt D-V13-1: a malformed task's performer is materialised before the refusal. TOOLS 5.2 CONVERSION MERGED 00:40Z (b4fcf58; user ruling 'Convert tools'): SmokeTest/CreateTaskAgg/ResetVrf/SetSimRate are proper 5.2 consumers (BridgeConfig, StackIdentity, output-tree-verified, one hash in the worktree; --help/--dry-run offline; ResetVrf lost its baked-in appNo 3299 - now exit 2 without one); LIVE JOIN GATE OWED (progress.md has the five-command block; needs 5-7 fresh appNos). Next: G-A rerun after the pausesim/V4b/route-shift lanes land; then V5 (Q5 pause + kill) and the western AG probe the research names. | none |
| L14 | Scenario preparation from an AO | One command that turns an AO into a READY-to-push VRF scenario (nav data + terrain fixture + type map/mode profile + tile cache), so `push order` runs immediately instead of an unattended overnight prep wait. | REQUIREMENT RECEIVED 2026-09-14 (user): 'one cannot simply push any scenario and get a simulation going ... the capability to pass the AO, get a VRF scenario generated so simulations can then be quickly executed on order push'. Prep today = nav data (vrfNavGenerator, 3.3 h for the 41x54 km COA-STP1 area), terrain .mtf copy with the navData record (FixtureGen), .scnx fixture (SMS incl. the custom including SMS, frame mode), relocated appData (loadAllNavigationDataOnTerrainLoad 1), type map / mode profile (R5), pre-flight tile cache. Target = one command `prepare --ao <bbox|init>`: cached per AO, resumable, progress-reported, ends READY; then `push order` runs immediately. Jira epic created 2026-09-14: STP-802 (scenario prep from AO; items STP-803 nav-data service, STP-804 terrain/fixture/config generation, STP-805 tile cache warm-up, STP-806 one-command prepare + READY gate). | design doc after the demo-critical lanes; reuse tools/FixtureGen, tools/navdata, APPDATA_RELOCATION, preflight cache | PLAN | none |

## 1b. Jira (created 2026-09-14 ~12:00Z, project STP; tracker STP-621 carries the status comment)
STP-795 bug: a blank line in a pushed message kills the SDK STOMP pump (G7 attempt 1). G7 attempts 1-2 void (STP-790 comment);
attempt 3 = tank platoon performer. Task-vocabulary epic created 12:55Z (see Jira).
STP-777 epic C2SIM reporting: STP-778 B5 false deserialize error, STP-779 B3 platform-unit name truncation, STP-780 B1
TaskStatus codes, STP-781 B2 push loss, STP-782 B8 re-announce, STP-783 B6 watchdog landing, STP-784 B7 heading+speed,
STP-785 B4 pre-flight port, STP-786 B9 bundle run, STP-787 B10 STP questions (+ STP-615 strict-parse check).
STP-788 epic nav-mesh refusal: STP-790 G7, STP-791 G7b, STP-792 G8, STP-793 confirming slope test.
STP-789 epic harness: STP-794 A2 detached watchdog. B7 and B4 dispatched 12:05Z (worktrees off f052ea7). B7 DONE ~13:20Z (feat/heading-speed @ 3ec0173: HeadingAngle from orientation via DtGetHeadingFromGeocentric, Speed = topographic ground speed, TaskCompleted.success forwarded natively, 30 report checks; live: moving/frozen speed values, heading sense). B4 DONE ~13:35Z (feat/preflight-port @ 9fe51f8: C# port of tools/preflight/leg_check.py, fixture parity 42 tasks / 48 legs / 14 flagged, ratio deviation 1e-14, 14/14 observation bodies byte-identical, wired in ExecuteTaskOnTick behind Vrf:PreflightWarnings, warnings only, ground units only; live: HTTP tile fetch path, a real order). STP-777/784/785/790/792/794/795/797 commented today.
STP-801 bug: STP export defect, MapGraphicID off (rulings review M5, REVIEW_RULINGS_5c67d41_2026-09-14.md). STP-802 epic scenario prep from an AO: STP-803 nav-data service, STP-804 terrain/fixture/config generation, STP-805 tile cache warm-up, STP-806 one-command prepare + READY gate. STP-809 follow-up (facade exposes back-end status Paused/Playing/gone so a dead back end does not freeze task time under Q5).

## 2. Serial spine (the only ordering that matters)
1. L4 lands (hardened runner) and L3 merges + main rebuilds (optional for G7; pinned either way).
2. G7 runs on an idle machine, foreground-polled, ~25 min: ONE variable (L1), instrumentation only
   otherwise. Harvest by an Opus executor against the prereg within the hour.
3. G7 HOLDS -> the router is finally exercised: the FINDING's confirming test (does the mesh route around the
   face?) reads directly off the same run. G7 MISSES -> G8 (gamewareMemorySize via --appDataDir), same day.
4. L2 builds land on a branch in parallel and ride the NEXT rebuild after their review.

## 3. Today's expected timeline (UTC)
11:30 all lanes running | ~13:00 L4 reviewed + committed, L3 merged, G7 prereg written (user OK on the SMS
location if needed) | ~13:30 G7 | ~14:15 harvest + verdict | afternoon: L2 builds, G8 if owed.

## 4. Dissent / risks
- L1 rests on the docs' thin contract for the query (no distance or sector bound is documented anywhere);
  the falsifier for the abstract-graph hypothesis is a G7 that still prints 0 points - then memory (G8).
- A custom SMS is the vendor's upgrade-safe mechanism, but whether a .scnx accepts an ABSOLUTE SMS path is
  unverified (build_fixture.py:605 uses $(DATA_DIR)); if not, the SMS must sit under C:\MAK\...\data and the
  user sanctions it as the fixture deploy was sanctioned.
- The runner's killer is unidentified; L4's backstop makes the consequence bounded, not the cause.
