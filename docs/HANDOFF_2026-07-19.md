# HANDOFF - end of 2026-07-19. Read this first, then the plan of record.

Written for a FRESH CONTEXT. Everything load-bearing is in tracked docs; nothing important
lives only in the conversation this replaces. *** BUT NOTE: runs/ IS GITIGNORED. The DOCS
are tracked, the EVIDENCE is not. A fresh clone cannot re-derive a single number here. If
you need to check a claim, you need this machine's runs/ directory. ***

## 0. Read in this order

1. THIS FILE (10 minutes) - state, traps, and what NOT to redo.
2. `docs/VRF_GROUNDWORK_PLAN.md` sec 1a (THE HEADLESS MANDATE), then the Status TOP entry
   dated 2026-07-19 LATE.
3. `docs/HEADLESS_RUN_PLAN.md` sec 4a - the RATIFIED pass criterion, including AMENDMENT 1.
4. `docs/experiments/RUN_2026-07-19_MOJAVE_CHAIN.md` and
   `docs/experiments/PREREG_TSK_DELIVERY_2026-07-19.md` - the two result write-ups.
5. `docs/RUNBOOK.md` secs 0 / 0.5 / 0.5.7 / 0.5.9 / 7 / 8.

## 1. THE HEADLINE

**The one-button headless loop WORKS.** `scripts/RunC2SimScenario.ps1` takes a C2SIM init
plus order through VR-Forces launch, interface join, unit creation, tasking, telemetry
capture and clean resign with ZERO human interaction. THREE fully unattended runs (161438Z, 202349Z, 222134Z); a fourth reached a full trace but its teardown failed and was finished by hand, so it does not count. An earlier version of this line said "four" and was written while one of them was still in flight.

**TWO of three units do not execute their tasks. THE THIRD DOES, AND WE NEVER WATCHED IT
LONG ENOUGH.** *** THIS SECTION WAS REWRITTEN 2026-07-19 LATE. Its first version said the
third unit "moves ~174 m and then stops" and that was FALSE - caught by two independent
clean-context audits and verified by hand. ***
- 114.MechCoy and 1.BdeHQ: 0.0 m - lat/lon BIT-EXACT in every run (1.BdeHQ bit-exact on all three columns; 114.MechCoy's ALTITUDE flickers 1116.6/1116.7 in 2 of 4 runs). Sample counts are 76/76/75/77, not a flat 76. THE FROZEN CONCLUSION STANDS, inside a window where the
  third unit demonstrably moved. GENUINELY FROZEN. This STANDS and is the primary defect.
- 1222.MechPlt: moves EAST toward its objective at ~1.4 m/s and WAS STILL MOVING when
  observation ended. Final-leg speeds, on the THREE runs that have RPT at all (161438Z, 202349Z, 222134Z -
  144109Z has ZERO RPT lines, so no RPT claim covers it): 1.35->1.46, 1.39->1.49, 1.28->1.48
  m/s. IT WAS NOT SLOWING. (Careful: there are only THREE RPT fixes per unit per run = TWO legs, and leg 1 starts at task issue, so it likely contains spin-up from rest. Two legs cannot distinguish "still accelerating" from "reached cruise ~1.48 m/s". The conclusion is unaffected and arguably stronger - 1.48 beats the 1.4 used in the arithmetic - but do not claim acceleration.) No deceleration signature, no stop in evidence.
  Independent corroboration an audit found and the write-ups omit: a POS sample at t=159.9
  reads 4.58 m further east than the last RPT fix at t=157.1, i.e. ~1.6 m/s still.
- WHY THE "174 m" LOOKED REPRODUCIBLE: it is a MEASURED displacement over RPT's 124.2 s coverage, not a stopping point.
*** PRECISION FIX 2026-07-20 - THE EARLIER PHRASING WAS WRONG. ~174 m is a REAL MEASURED
DISPLACEMENT: 84.0 m + 90.1 m across RPT's coverage span t=32.9 to t=157.1 = 124.2 s.
It is NOT "the length of the observation window" - that window is ~145 s and at 1.4 m/s
would predict ~203 m, which is not what was measured. The correct statement is: the unit
covered a MEASURED 174 m and WAS STILL MOVING when RPT coverage ended. NEW, and not
previously recorded anywhere: RPT STOPS REPORTING ~23 s BEFORE THE POS TRACE DOES
(t=157.1 vs t=180.3). That gap is its own open question. *** Usable observation runs t~35 to t~180, because the interface RESIGNS at
  teardown and its objects are removed - the two uuids still readable AFTER the collapse are EXACTLY the two present at t=3,
  BEFORE the interface created anything (cde66adc, f864e51f). That is delta-independent and
  needs no clock alignment. *** DO NOT cite "tracks the app exit to within 0.3 s" - an
  earlier version did: the pairwise deltas from its own numbers are 0.5/0.0/0.2/0.3 (max
  0.5, not 0.3), and the app-exit column is not reproducible from the artifacts without
  assuming a per-run offset that increases monotonically, which reads as fitted. THE
  CONCLUSION SURVIVES; only that precision claim does not. *** THE COLLAPSE IS
  THE INTERFACE RESIGNING, not an oracle fault (a cold reader read it as a fault; that part
  of its finding is wrong).
  *** DO NOT CITE "StopIface fires at trace t=182.1s" as the proof - an earlier version of
  this doc did, and it is on a DIFFERENT CLOCK from trace time (trace t=0 is ~5.8 s after
  the WatchVrf stage starts, so StopIface is really trace t~176). At face value it made the
  cause follow the effect. The app-exit correlation above is the sound evidence. ***
  Two falsifiers were tested and cleared: the trace keeps sampling to t=679 (the instrument
  did not die), and the surviving readable=2 are the same two baseline uuids present before
  creation. `reflected=54` persisting is NOT a counter-argument - VrfFacade.cpp:657 returns
  an accumulate-only set with no removal callback, so that counter cannot decrease.
  ~1155 m at ~1.4 m/s needs ~825 s. Every run observed ~145 s.
- "DURATION IS NOT THE BINDING CONSTRAINT" - WRITTEN ELSEWHERE IN THESE DOCS, AND WRONG FOR
  THIS UNIT. PREREG_TSK_DELIVERY_2026-07-19.md:130 had it right first and the newer docs
  contradicted it. RUN LONGER: -RunSecs 900+. It remains RIGHT for the two frozen units.
- NO TASKCMPLT within the observed window - but that is now WEAK evidence. A completion at
  t=400 would be as invisible as an RPT at t=400. Do not cite it as proof of anything until
  a run observes long enough for a completion to be possible.

**The movement oracle itself is in question, and this outranks the movement result.**
See sec 3.

## 2. WHAT IS RULED OUT - DO NOT RE-INVESTIGATE THESE

Each was closed with evidence this session:
- Wrong or buried spawn. Creation is EXACT to six decimal places against the init, and the
  ground clamp works, but NOT at one altitude - the three taskees are ~10 km apart and clamp to DIFFERENT terrain heights: 1.BdeHQ 1131.4 m, 114.MechCoy 1116.7 m, 1222.MechPlt 1040.6 m. Do not quote ~1040 m for the group; it is one member's value (superseded wording follows) (ground clamp works (spawn resolves to ~1040 m terrain, not the requested) 10000 m MSL).
- Missing route or missing task issue. All three tasks produce CreateRoute + MoveAlongRoute
  in the app log, every run.
- Lying completions. None are emitted at all - BUT see sec 1: no completion could have been observed anyway, since nothing is visible past ~180 s. This is NOT a closed question; it is an untested one.
- Stale aggregate reads hiding real movement. 1.BdeHQ is an ENTITY with no members and
  moved zero bits; 114.MechCoy and all 37 co-located members hold ONE coordinate across 76
  samples.
- SCENARIO-injected behaviour (the user's hypothesis): TropicTortoise `.pln` is a 36-byte
  EMPTY header - which also CLOSES the groundwork 0.5 ".pln unparsed" gap. `.spt` scripts
  count=0, `.orb` empty, and the `.oob` defines exactly THREE objects, all with empty task
  lists and `independently-tasked False`. Bogaland2 is on disk, is TropicTortoise's
  ancestor, and is equally inert. THIS HALF IS SOLID - byte-verified twice.
- *** MODEL-SET DEFAULT BEHAVIOUR IS **NOT** RULED OUT. THIS WAS WRONGLY LISTED HERE AND IS
  RETRACTED (2026-07-19 late, found by a fact-check audit, verified by hand). *** The
  earlier claim that "`taskRules/` and `scriptedObjectMovement/` are EMPTY DIRECTORIES" is
  true ONLY of the `C2simEx` layer. `C2simEx.sms` ENDS WITH
  `(include "..\data\simulationModelSets\EntityLevel.sms")`, and the EntityLevel layer is
  NOT empty:
      EntityLevel/vrfSim/taskRules/       3 files - actionCategories.tsk,
                                          **default-task-rules.tsk**, **doctrines.dct**
      EntityLevel/scriptedObjectMovement/ 19 files
  NOBODY HAS OPENED ANY OF THEM. A file literally named `default-task-rules.tsk` in the
  include chain we load is exactly the kind of thing that could shape how a tasked unit
  behaves. The user's ORIGINAL hypothesis is not resurrected by this - the four "untasked
  movers" were positively identified as tasked-unit members - but the STATED BASIS for
  dismissing default behaviour was checking the wrong directory layer.

WHAT REMAINS, RESTATED after the audits: (1) TWO of three taskees are genuinely FROZEN
inside a window where a third demonstrably moved - that is the real, primary defect;
(2) the third MOVES and was never watched long enough to see whether it ARRIVES - untested,
not failed. The older blanket phrasing ("VR-Forces does not execute a well-formed
MoveAlongRoute") was true for two units and unsupported for the third.

## 3. THE BIGGEST OPEN ITEM - THE TWO ORACLES CONTRADICT

WatchVrf now emits `RPT` lines: VR-Forces' OWN position reports, carrying MARKING TEXT.
(That incidentally solves the member-to-parent mapping the runaway census called
unsolvable offline - names, not uuids.)

On 1222.MechPlt the channels disagree about DIRECTION. Objective is EAST.

    RPT (VR-Forces' own report):  steady EASTWARD progress, ~174 m, ~1.4 m/s, monotonic
    POS (our DR-extrapolated read): 65 m WEST of start, then frozen, sawtoothing +/-1.5 m

THE CONTROL IS WHAT MAKES THIS SERIOUS: on the two units that do NOT move, the channels
agree EXACTLY. They agree on stationary objects and disagree ONLY on the moving one. That
is a TRACKING failure, not two noisy sources.

POS displacement is the standing "the ONLY movement oracle" rule. Every negative movement
result this project has recorded rests on it - including the runaway/warp census.

ESTABLISHED: the channels contradict, with a control.
INFERRED, NOT PROVEN: that RPT is the truthful one. Not settleable offline.

THE DECISIVE TEST: log `lastSetLocation()` (raw, baseEntityStateRepository.h:118) beside
`location()` (approximator-extrapolated, :113) on the same row. It NEEDS A NATIVE CHANGE.
READ SEC 5 BEFORE ATTEMPTING IT.

## 4. TRAPS THAT COST TIME TODAY - DO NOT REDISCOVER THEM

- **`Start-Process -Wait` waits for the process AND ALL DESCENDANTS.** It can therefore
  NEVER return from a stage that launches VR-Forces. Cost a 47-minute deadlock. Use
  `-PassThru` + `$p.WaitForExit(ms)`. `Wait-Process` waits only for the named process.
- **A plain MSBuild of VrfBridge returns EXIT=0 in ~2 seconds having compiled NOTHING.**
  Always `/t:Rebuild`. This is a false green of the same shape as the retracted
  `reflected>0` criterion.
- **`grep -P` SILENTLY DEGRADES on this machine's locale** and reports a false clean for
  the ASCII check. Verify in PowerShell with a byte scan. Worse: `grep -P ... && echo CLEAN
  || echo CLEAN` fires the fallback on the ERROR, not on success.
- **`Parser::ParseFile(path, [ref]tokens, [ref]errors)`** - swapping the last two reports
  thousands of "parse errors" that are actually tokens.
- **`[IO.File]` uses the PROCESS working directory, not PowerShell's `Set-Location`.** It
  resolved a path into the FROZEN C++ ORACLE repo once. Always pass absolute paths.
- **`$PID` is read-only** in PowerShell; assigning to it errors per-iteration.
- A `Where-Object { $_.CommandLine -like '*needle*' }` process query MATCHES ITS OWN
  PROCESS, because the needle is in the querying command line. Build the needle at runtime.

## 5. THE NATIVE CHANGE - WHAT WENT WRONG, AND THE RULES FOR REDOING IT

I attempted the raw-vs-DR diagnostic natively. IT BROKE THE PIPELINE TWICE AND WAS
REVERTED (commit 5d14eda). The four-run single-variable table:

    bridge      run        "No backends"  MoveAlongRoute  POS lines
    A48ABE6C    144109Z          0              3           4512
    A48ABE6C    161438Z          0              3           4512
    61FE865C    185814Z          0              0              1   (WatchVrf crashed 0xC0000005)
    4FF31875    193252Z          6              0              0   (object creation FAILED)
    A48ABE6C    202349Z          0              3           4464   (rollback CONFIRMED)

ROOT CAUSE OF THE REGRESSION: the change made `Start()` register additional controller
callbacks UNCONDITIONALLY, for every consumer INCLUDING the production app. The executor
FLAGGED this in review as "a real behaviour delta for VrfC2SimApp, not purely additive"
and the supervisor accepted it anyway. The brief was at fault: it asked for capabilities
and never said they must not alter the default `Start()` path.

**A DIAGNOSTIC MUST NOT CHANGE THE BEHAVIOUR OF THE THING IT OBSERVES.**

RULES FOR THE NEXT ATTEMPT:
1. Additive only. New function; do NOT widen `vrf::Geodetic` (five consumers share it).
2. OPT-IN only. Nothing new may run on the default `Start()` path.
3. Validate against the four-run table above BEFORE trusting any new build.
4. Back up all 7 `VrfBridge.dll` copies first - NONE are committed, so rollback means
   rebuilding from a commit, not `git checkout` of a binary.
5. Redeploy to all 7 consumer dirs and confirm ONE hash. They were THREE different builds
   before being unified today.

## 6. FINDINGS THAT SURVIVED THE REVERT - keep these, they are implementation-independent

- **RUNBOOK sec 7's "RTTI across the MAK DLL boundary" story is FALSE.** MAK's own header
  `reflectedExtAggregate.h:15-19`: under `DtHLA` the class deliberately derives from
  `DtReflectedObject` instead of `DtReflectedAggregate`, so a null
  `dynamic_cast<DtReflectedAggregate*>` is the cast behaving CORRECTLY. No RTTI mechanism
  is involved. Corrected in RUNBOOK.
- **The blind `static_cast` worked on aggregates only through ACCIDENTAL VTABLE SLOT
  ALIGNMENT** - all three reflected kinds declare their state-rep accessor as the first new
  virtual after the destructor. On a CONTROL object the same slot is a disjoint branch,
  which is exactly why `location()` returned garbage there while `lastSetLocation()`
  faulted. One mechanism, both symptoms.
- **"The TropicTortoise baseline objects are POSITIONLESS" (RUNBOOK 0.5.7) is WRONG as a
  statement about REFLECTED values.**
BASELINE-OBJECT CENSUS - re-derived from every trace 2026-07-20. This claim has been WRONG
FOUR TIMES; these are the counted values, not a summary of them:
  d39a55ad GlblTerrDmg  0 samples - NEVER REFLECTS AT ALL. That is why readable=2 of
                        reflected=3. Its position has never been read because it never
                        reflects.
  f864e51f GlobalEnv    1388 samples, TWO value-forms: "NaN,-90.000000,NaN" and
                        "0.000000,-90.000000,6.4e72". NEVER the authored 9e-6.
  cde66adc Page-In Area 1390 samples, FOUR value-forms for a STATIC object:
                        "90.000000,-90.000000,0.0", "NaN,-90.000000,NaN",
                        "0.000001,-90.000000,1.02e15", "0.000001,-90.000000,6.4e72".
THE BAD CAST CORRUPTS BOTH READABLE OBJECTS, NOT ONE. Neither one's true position has ever
been read. Authored position is a separate question from reflected value: GlblTerrDmg and
GlobalEnv ARE authored at null island, but that does NOT make their readings faithful.
A NaN row from GlobalEnv is NOT correct-and-expected.
CONSEQUENCE for the native re-attempt: a control-object-aware accessor is needed for BOTH
readable objects. Note the absurd altitudes (1.02e15, 6.4e72) - that is the column whose
omission produced the documented ORACLE-GATE false green.

## 7. TOOLING HARDENED TODAY

- Argument guards on every tool. `StopIface` previously ACTED DESTRUCTIVELY WITH NO
  ARGUMENTS; it now requires `<restUrl> <stompUrl> --yes` and has no defaults. Contract:
  0 success / 1 operational failure / 2 usage error with NOTHING done.
- The runner's `Start-Process -Wait` deadlock (sec 4).
- **The oracle gate PASSED ON GARBAGE** - `lat 1e-6, lon -90, altitude 1.02e15 m` - because
  it only checked lat/lon and never looked at the altitude column it was already given. Now
  checks altitude sanity and an equator/null-island placeholder.
- **A crashed oracle now FAILS the run.** It previously printed "RUN COMPLETE - evidence
  collected" and exited 0 after WatchVrf died three seconds in.
- StopVrf: back-end graceful-close fallback, and NESTED modal handling (sec 8).

## 8. STATE OF THE STOPVRF FIX - VERIFICATION PENDING

A `"Session Status - The current session has ended. Close current terrain?"` modal fires on
SOME runs and hung teardown once. *** "EVERY" IS FALSIFIED - but NOT by log absence, which is invalid evidence
here: the 193252Z log, from the run where the modal DID appear, also never names "Session
Status" (it logs a generic "VR-Forces" window), because the top-level search structurally
could not see it. THE VALID EVIDENCE IS THAT FOUR TEARDOWNS COMPLETED CLEANLY. It appeared on a run
that FAILED. It is INTERMITTENT. *** Separately, the BACK-END GRACEFUL
FALLBACK fired FIVE of five times on runs that HAD the feature (144109Z predates it
entirely). 193252Z DID fire it and the back-end exited; that run then failed for a
different reason - "vrfGui has no main window to close". THE GUI QUIT HAS NEVER CARRIED
THE BACK-END WHERE IT COULD BE OBSERVED - this is the path, not an exception. THE MODAL (not the fallback) is a NESTED DESCENDANT of the vrfGui
main window, so StopVrf's top-level search structurally could not see it. Fixed to scan
descendants, answer "No", log every nested window found, and REFUSE to press anything on an
unrecognised dialog.

*** VERIFICATION RESULT, run 20260719T222134Z: PARTIAL. READ THIS CAREFULLY - THE RUN
EXITED 0 BUT THE THING UNDER TEST NEVER FIRED. ***

VERIFIED: the back-end graceful-close fallback. The StopVrf log says "The GUI quit alone
did not carry the back-end on this run", the fallback close was accepted, the back-end
exited within 30 s, no force-kill, all three RTI processes preserved, StopVrf EXIT=0.

*** NOT VERIFIED: the nested-dialog handling. *** The "Session Status" modal DID NOT APPEAR
on this run. There is no `answered No to "Session Status"` line - and, more troubling, NO
`nested window` lines AT ALL, even though the new code is supposed to log every nested
window it finds INCLUDING the QDockWidgets that are always present. So it is not merely
that the dialog was absent; THERE IS NO EVIDENCE THE DESCENDANT SCAN EXECUTED. *** BOTH OF THE EXPLANATIONS THIS DOC ORIGINALLY OFFERED ARE REFUTABLE FROM THE SOURCE -
corrected 2026-07-19 late. THE SCAN CERTAINLY RAN: `$nestedScanDue = Get-Date` (StopVrf.ps1
~:454) makes the throttle due on the FIRST iteration, the scan block (~:482) is
unconditional in the loop body with no break/continue past it, and the 222134Z log proves
the loop iterated at least once because it detected and answered "Are You Sure?".
THE TWO SURVIVING EXPLANATIONS ARE BOTH SILENT-FAILURE PATHS inside Get-VrfNestedWindows:
    try { $root = ...FromHandle($w.Handle) } catch { continue }        (~:330)
    try { $kids = $root.FindAll(...Descendants, $winCond) } catch { continue }   (~:332)
Either the descendants query genuinely returned zero Window elements on a healthy GUI
(which would contradict this RUNBOOK's own claim that it finds "Session Status plus three
QDockWidgets"), or it threw and was swallowed with NO log line.
THE DISCRIMINATING FIX IS NOT "look for nested window lines" - a swallowed exception
produces an identically empty log and you would wrongly conclude the scan is not running.
LOG THE CATCH BLOCKS AND $kids.Count. ***

CONSEQUENCE - DO NOT INHERIT THIS AS WORKING. The dialog is INTERMITTENT (it appeared on
run 193252Z and not on 222134Z), which is the same variability already recorded for the
"Quit All Back-Ends" checkbox. An intermittent blocker plus an unexercised fix means the
next unattended teardown can still hang. Before trusting it: confirm the scan runs at all
by looking for `nested window` lines in a normal teardown, then wait for a run where
"Session Status" actually appears.

A GREEN RUN WHERE THE THING UNDER TEST DID NOT FIRE IS NOT A PASS. This is the same
false-green shape that produced the other defects listed in sec 7.

## 9. NON-NEGOTIABLES (unchanged, and they all held today)

- NEVER force-kill a JOINED federate. NEVER kill rtiAssistant / rtiexec / rtiForwarder.
  A modal ON the assistant was DISMISSED via UIA, not killed, and the assistant was then
  confirmed healthy by a ResetVrf --dry-run.
- Every appNo comes from the single `*** NEXT FREE: <n> ***` marker in
  OPUS_EXECUTION_PLAN.md Appendix B, LEDGERED BEFORE the join. Search by FORM, never the
  bare string. Unconsumed numbers are BURNED, never recycled.
- ASCII-only in tracked files.
- The C++ repo `c2simVRFinterfacev2.36` is a FROZEN ORACLE. Do not modify it.

## 10. THE NEXT DECISION

Two paths, supervisor's lean recorded:
- **(0) RUN LONG ENOUGH - DO THIS FIRST. IT IS FREE AND IT HAS NEVER BEEN DONE.**
      pwsh -File scripts\RunC2SimScenario.ps1 -RunSecs 900
  ~825 s of motion is needed and every run so far observed ~145 s (sec 1). One run settles
  three open questions: does 1222.MechPlt ARRIVE, does a TASKCMPLT ever fire, and do the
  two frozen units stay frozen over a long window. THIS SUPERSEDES THE OLD "(a) IS THE
  LEAN" - an audit caught this section still recommending (a) while sec 1 of this same file
  said RUN LONGER.
- **(a) RPT-first, no native work.** VR-Forces' own named position reports already exist and
  already produced the oracle contradiction. Push on the movement defect with them. Keeps a
  working pipeline. (Was "THE LEAN" until option 0 was identified.)
- **(b) Re-attempt the native raw-vs-DR diagnostic** under the sec 5 rules. Higher value if
  it lands - it would settle whether every past negative movement result is trustworthy -
  but it broke the pipeline twice today.

Still open and NOT bundled into anything: type-adjudication Q1 (country-by-side) remains
AWAITING USER PICK in TYPE_GAP_ADJUDICATION.md. It does not gate the Mojave work.
