# PREREG V7 - N2d on the MojaveAO20 terrain: is the western refusal the oversized area's fragmented abstract graph? (written 2026-09-15 ~01:05Z, BEFORE the run)

Tier HEAVY (a cause claim: the research's C1). Gate: PREREG (this). Docs consulted: docs/experiments/RESEARCH_ABSTRACT_GRAPH_CONNECTIVITY_2026-09-15.md
(UG52 66.2 p1276 "maximum size for a navigation area using the default values is 20 km by 20 km"; navigationProfiles.mtl max-cells 1000;
the generation log's ABSTRACT GRAPH POST PROCESS REPORT ratio nbrs/(nodes-1): COA 254/8,856 sectors < 0.5 clustered on 1-35's lane -
destack start 0.11, N2d start 0.29, V0's sector 1.00, east 1.00; AO20 0/1,600; the vendor-log count grep for Gameware runtime
warnings = 0 in five logs, killing the runtime-limit candidate C2); PREREG_N1_N2 secs 5.6/10.3 (N2d: slot moves 4/6, every long goal
refused, the unit drove V0->V1->V2->V3 on straight paths); G7B_G8_RESULTS (east: AG 8/8; flat 1.9 km 4/4, 5 km 1/4 - no 23 m ceiling).

## The run - ONE variable vs N2d
Fixture R9_Mojave_Empty_52_NavAO20_AG_S2: FixtureGen --empty with --terrain "tools\navdata\out\MAK Earth (online) + MojaveAO20.mtf"
(the navData record of the 20.08 x 19.95 km area generated 2026-09-07; both file and navData exist on disk) and the SAME SMS
C2SIM_EntityLevel_AbstractGraphs_Slope2; validated by validate_fixture; deployed to the sanctioned userData\scenarios. Init
COA-STP1_Initialization_N2d.xml (1-35 at 34.657894/-116.745512), order PROBE_RIDGE_1-35_Order.xml, AtOrder + nav-area gate, 900 s,
member consoles 4 - the N2d line with only --scenario changed (scratchpad n1n2/v7_launch.sh). Geometry, pre-registered from the
research: the N2d start, the destack start, the P11 freeze point and V0 are INSIDE AO20 (sectors of ratio 1.00 there); V1, V2 and V3
are ~206 m OUTSIDE AO20's extent.

## Predictions (a missed HIGH is a STOP)
- **P-A (HIGH, instrument):** the trace's nav-area rows name "NavArea-ground-platform MojaveAO20", never MojaveCOA; the ready gate
  fires on an AO20 row. MISS -> the fixture did not take the terrain; STOP, read the .scn.
- **P-B (HIGH, the test):** the ~30 m formation-slot moves at the N2d start PLAN for 6 of 6 members ("Planned path has N points", N > 1),
  against 0/6 at the old spot on COA and 4/6 at this spot on COA. MISS (<= 4/6) -> C1 is NOT the whole story at this spot; STOP.
- **P-C (HIGH, the discriminator):** the V0 goal (2.3 km, inside AO20, destination sector ratio 1.00) PLANS for the leader - refused
  on COA in N2d. MISS -> C1 dead for long goals; the carried second mechanism (per-query working memory / concurrency) becomes live.
- **P-D (MEDIUM, the self-validating control):** the V1 goal (206 m outside AO20) fails the `Is destination in nav area?` GATE
  ("fail in action Is destination in nav area?"), NOT the mesh query - a different console signature from "(0) points"; the tree then
  drives the straight feature path to V1 exactly as N2d did (V0->V1 line, north of the face). If instead V1 is mesh-planned,
  record it (the area extent would be larger than the research read).
- **P-E (RECORDED):** query latency of the planned V0 leg vs the COA refusals (0.4-1.3 s "fast no" on COA; AO20 successes 2.4-4.2 s
  in G1-G3); whether the leader again completes the route; sim ratio.
Both P-B and P-C HOLD -> C1 CONFIRMED: the oversized area's generation shattered the western abstract graphs; the product rule
(cap at 20 x 20 km, tile the AO, gate on the connectivity ratio) is adopted for STP-802/803 and the COA area is retired for
navigation. P-B holds and P-C misses -> a long-range mechanism remains; the G7b-run-A same-second anomaly is the next object.

## Confounds
Cold nav-data load of a different area (the warm step in the launch line targets the COA navData; the AO20 area may load cold -
the ready gate covers 300 s; the members are created at order push, after the gate); the AO20 area was generated on 2026-09-07
(different vendor terrain streaming state than COA's 2026-09-13 generation) - if P-B/P-C hold, the fragmentation ratio (0 on AO20)
is the documented mechanism; a generation-date effect cannot be separated in this run and is recorded as a residual.

---

## RESULTS - run 20260915T011237Z_run (launched 01:12Z; order pushed 01:14Z at the ready gate; window 900 s to its cap; ONE change vs N2d - the nav area)

**ALL FOUR SCORED PREDICTIONS HIT. C1 CONFIRMED.** The same start, init, order, SMS, build, type map and appData that
produced "Planned nav path has not enough (0) points" for every long goal on MojaveCOA six hours earlier produced ZERO
refusals on MojaveAO20: the string "not enough" occurs 0 times in 846,600 trace lines; 14 goals were mesh-planned, 18
fell through the area-bounds gate to a 1-part straight path, and 32 of 32 goals are accounted for with no refusal.
Harvest: plan_counts.py, area_timing.py and n2b_harvest.py on the run dir (n2b's axis constants left on the old
destack-start axis for comparability), plus extra.py (sim clock, per-goal latency) and crawl.py; outputs in the
scratchpad under v7harvest/.

| pred | predicted | measured in this run | verdict |
|---|---|---|---|
| P-A (HIGH, instrument) | area rows name MojaveAO20, never MojaveCOA; the ready gate fires on an AO20 row | "MojaveCOA" 0 hits / "MojaveAO20" 67 hits in watchvrf-trace.csv; all 31 "New Primary nav area" rows across 7 objects read "NavArea-ground-platform MojaveAO20"; runner stage 7d: "NAV AREA ACQUIRED after 0s of gate: object 2/1_AD/25_~PXY took primary area NavArea-ground-platform MojaveAO20" | **HIT** |
| P-B (HIGH, the test) | the ~30 m formation-slot moves plan for 6 of 6 members (baseline 0/6 at the destack start, 4/6 at this spot on COA) | 6 of 6, first subtask, "Planned path has N points": M1A2 1 = 3, M1A2 2 = 5, M3 1 = 9, HMMWV 1 = 2, HMMWV 2 = 4, M577A2 1 = 4; 0 refusals for any member (plan_counts 0pts column all zero) | **HIT (6/6)** |
| P-C (HIGH, the discriminator) | the V0 goal (2.3 km, inside AO20) PLANS for the leader - refused on COA in N2d | leader goal wall 38.40 / sim 59.6, destination 34.67998/-116.72480 = V0; "Node Is destination in nav area?: success", "C2SIM override ground-vehicle-move-to.lua: useAbstractGraphs=true", "Job Calc off road nav path part success", then "Planned path has 30 points." at wall 38.80 / sim 61.4. All five other members mesh-planned their own 2.3 km leg as well: 28 / 31 / 32 / 36 / 40 points - 6 of 6 | **HIT** |
| P-D (MEDIUM, control) | V1 (206 m outside AO20) fails the "Is destination in nav area?" GATE, not the mesh, and the tree drives the straight path | leader goal wall 102.60, destination 34.65121/-116.81164 (0 m from V1): "Node Is current point in nav area?: success", then "fail in action Is destination in nav area?", then "Planned path has 1 parts." - never a "(0) points" line. REFINEMENT: V2 (wall 250.2) and V3 (wall 492.5) failed the CURRENT-POINT gate instead ("fail in action Is current point in nav area?"), because by then the unit itself was outside AO20 - still the gate, never the mesh. The unit then drove V1 (task complete sim 1,348) and V2 (sim 2,787) on those 1-part paths | **HIT + refinement** |
| P-E (RECORDED) | query latency vs the COA refusals; route completion; sim ratio | goal row -> plan row 0.40 s wall for BOTH the 18 m slot move (35.10 -> 35.50) and the 2.3 km V0 leg (38.40 -> 38.80). COA's refusals were a 0.4-1.3 s "fast no" and the G1-G3 AO20 successes 2.4-4.2 s, so a WARM AO20 answers a 2.3 km query as fast as COA said no: latency does NOT discriminate success from refusal, and the "fast no means there was no graph to search" heuristic is withdrawn. Leader completed V0, V1 and V2 and was 1,036 m short of V3 at the cap (N2d: 0.85 km short, but at 5,820 sim s). Sim ratio 5.70x (5,296 sim s in 929.2 wall s; N2d ~7.5x). 12,136 C2SIM reports captured | **RECORDED** |

Also recorded, as asked:
- **Nav-area gate: 0 s of gate.** The AO20 area was resident before the order was pushed - the launch line's read-only warm
  of the AO20 navData paid; -PreOrderSettleSecs 240 was not spent. The gate object was the proxy 2/1_AD/25_~PXY at wall
  31.9, before any member existed (members are created AtOrder; first member fix at wall 36.9).
- **Members query the area BEFORE their own area row.** The first member queries, at wall 35.3-35.4, all succeeded; the
  members' own "New Primary nav area" rows land at 35.7-37.4. On this area residency is a simulation-wide property, not a
  per-object precondition - so gating the order push on ANY object's row is a sound ready signal.
- **Zero crash / tick-phase / MissingMethod / unhandled-exception lines** in vrfc2simapp.log (124 MB) and in the runner
  log: the second live confirmation of gate G-A after N2d.
- **Stop rule: -StopWhenComplete did NOT fire**, as expected - the task's Duration is PT2H at scale 1.0. Runner line:
  "BLOCKED BY condition (1) taskee coverage: 1 of 1 order taskee(s) have NO terminal report ... 0 terminal of 1 tasks";
  the window ran 900.6 s of its 900 s cap.
- **Teardown clean (15th).** StopIface EXIT=0 (server to UNINITIALIZED), VrfC2SimApp exit 0 (clean resign), both observers
  resigned on the stop file and were never killed, StopVrf graceful with no /F, no VR-Forces process left, rtiexec 69856
  and rtiForwarder 50520 preserved; the detached watchdog saw runner.teardown-ran and stood down without touching anything.
- **UNEXPLAINED AND CARRIED - the column crawls to a stop on the final, off-mesh leg.** From sim ~3,150 the 200 s speed
  bins fall from ~10 m/s to 1.18, 0.80, 0.65 ... 0.34 m/s; M3 1 stops outright at 34.58197/-116.98336, 2,490 m short of
  V3, after a 43 m climb between sim 3,100 and 3,200, and advances -2.5 m over the last 600 sim s while the other five
  creep at ~0.5 m/s some 1.0-1.2 km from V3. That leg was NEVER mesh-planned (the current-point gate failed for it), so
  this is the carried stall mechanism - slope/soil, or formation mutual speed control - and not a navigation-query
  property. It neither supports nor refutes C1, and it is why the route again did not close inside the window.

**Adversarial review.** The strongest competing reading is that the probe moved TWO variables at once: the area's SIZE
(41 x 54 km, ~2.3 GB, 5.5x the documented maximum) and the CONNECTIVITY of the abstract graphs its generation produced.
A purely RUNTIME size effect - C2's "Failed to allocate %i bytes for the AbstractGraph. it won't be inserted" /
"This traversal has reached its maximum working memory size" path - explains every number above exactly as well as
fragmented generated data does. THIS RUN DOES NOT REFUTE IT and cannot: the two are confounded by construction, because
swapping the area swaps both. What refutes C2 is offline and already in hand - the research's count grep for those
Gameware runtime strings returned 0 hits across five vendor logs, and G8's gamewareMemorySize 16 -> 128 changed nothing
(weak on its own, since the mtl says that value is per block). The clean separation is still owed and is offline, not a
run: a fresh 20 x 20 km generation over the SAME western ground - if it too yields sectors with ratio < 0.5, size is not
the operative variable. Second competing reading, "the unit completed the route because the mesh steered it": REFUTED
here - only the 2.3 km V0 leg was mesh-planned; V1, V2 and V3 were 1-part straight feature paths after a gate failure,
exactly as in N2d, so the completion is the same LINE property sec 10.3 recorded and not a mesh effect. Third, C3 "the
start sits on an isolated mesh island": refuted as a primary cause - same position, same ground, 6/6 short plans and a
2.3 km plan; under C1 an island is a consequence of fragmentation, and the consequence vanished with the area. Residual
the prereg pre-registered and this run cannot close: AO20 was generated 2026-09-07 and COA 2026-09-13, so a
generation-date / terrain-streaming-state effect rides along with the size change.

VERIFIED (this run's own files only - watchvrf-trace.csv, vrfc2simapp.log, reports-captured.log, the runner log and the
teardown logs; no vendor sim log was opened): every figure in the table and in the bullets above.
ASSUMED (not measured here): that MojaveCOA would still refuse today - the COA baseline is N2d at 22:45Z on the same
build, not a same-session control; and that the AO20 navData under the user TEMP tree is byte-identical to what the
2026-09-07 generation wrote (its presence and its load were verified, its hash was not).

**VERDICT: C1 CONFIRMED.** The western refusal is a property of the oversized area's GENERATED navigation data, not of
the ground, the start position, useAbstractGraphs, load timing or query distance - so the product rule is adopted for
STP-802/803: cap every generated nav area at the documented 20 x 20 km, tile the AO, and fail generation offline on any
route sector whose Average Neighbor Node Count / (Average Node Count - 1) is below 0.9; MojaveCOA is retired for
navigation. The carried second mechanism (G7b run A's same-second success and refusal for co-located vehicles) is
untouched by this run and stays open, as does the off-mesh stall that stopped M3 1 short of V3.
