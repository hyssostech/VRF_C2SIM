# PREREG - R9 ORDER on 5.2: do the tasked units MOVE along their routes and report TASKCMPLT?

Date 2026-09-05. Tier HEAVY (first C2SIM ORDER to drive movement on 5.2; adjudicates whether the
migrated tasking path works). Written BEFORE launch. NOT YET RUN.

## 0. Citations this rests on (CLAUDE.md gate: own record + vendor)
OWN: docs/RESEARCH_52_MOVEMENT_ORDER_2026-09-05.md (G1-G4, the 5.2 movement basis, seat-verified);
PREREG_PLACEMENT_R9_52 (placement confirmed 44/44 on terrain, entity-level, def8a5c);
docs/VRF_5.2_DECISION_EVIDENCE.md Y-10 (MoveAlongRoute), Y-12 (autonomy on), Y-13 (armour off-road,
near 15 m); docs/VRF_5.2_MIGRATION_DIFF.md D1-D8; the route-freeze fix 726f762 (route-by-uuid).
VENDOR: formationState.h:52-53 (formation valid = promotion map non-empty; RN VRF-8968 waits for
it); disaggregatedMoveAlongController.h:46-49 + disaggregatedManeuverAlongController.h:173-178,
:285-292 (unit completion = ALL subordinates + stuck fail-safe); moveAlongTasks.h:26-30 (entity =
last vertex), :79-82 (route by uuid); EntityLevel ground-tracked.sysdef move-along
(near-distance 15 / at-distance 1.0), maneuver-in-formation (near 15 / at 2.0), read by the live
atDestination() (groundMoveToDestinationControllerComponent.h:157-165); taskCompleteReport.h:68-102
(success()/taskId()/taskTrackingNumber(), per task); UG52 23.3 p505 (abeam arrival), 30.24 p599.

## 1. Frame
5.2 stack, rtiexec posture, fixture R9_Mojave_Empty_52.scnx (EntityLevel), server c2sim-server-vrf.
Init = R9_Mojave_Lean_Initialization.xml (6 units created, placement proven). Order =
R9_Mojave_UnitMove_Order.xml: THREE tasks, three taskees - T_R5_TK1 -> 1.BdeHQ (ENTITY, M1A2),
T_R5_CO1 -> 114.MechCoy (AGGREGATE, Tank Company), T_R5_PL1 -> 1222.MechPlt (AGGREGATE, Tank
Platoon); each a 2-point route. The other three units (1141/1142/1143.MechPlt) are created but NOT
tasked. PlacementAglSet stays PRODUCTION-DEFAULT (true) - this run is the production path, not the
create-isolation control. ONE variable vs the placement run: an order is now pushed.

## 2. Procedure (create-before-task is MANDATORY, G4)
Ledger 4 appNos (sim, watch, app, spare). STEP 1-4 exactly as PREREG_PLACEMENT_R9_52 A1.6 (env,
StartRtiExec52, LaunchVrf52 -Scenario R9_Mojave_Empty_52 -NoGui, WatchVrf 300 s 1 s --diag BEFORE
the init). STEP 5 PushInit. STEP 6 launch the app (Debug logging so the name->uuid + create lines
show). **STEP 6b GATE: confirm all 6 taskees created (WatchVrf ent+agg plateau AND the app's
PLACEMENT summary + per-object ObjectCreated) BEFORE pushing the order** - an order that beats
creation is DROPPED, never retried (G4). STEP 7 PushOrder R9_Mojave_UnitMove_Order.xml. STEP 8
let it run >= 180 s (routes are short; movement + settle). STEP 9 teardown (StopVrf52; RTI
untouched). STEP 10 score with tools/analysis (movement_check per Lane A).

## 3. Predictions - each names its doc + falsifier
P1 CREATION UNCHANGED (HIGH): placement repeats - 6 units on terrain, DROPPING TASK == 0,
   ABANDONING TASK == 0. Falsifier: any drop/abandon -> the create-before-task gate failed (G4);
   STOP and widen the gate, do not read movement.
P2 TASKS DISPATCH (HIGH): all 3 tasks issue MoveAlongRoute by route uuid; 3 creation-line routes
   with the 99-char names (route-by-uuid, 726f762). Falsifier: `Can't find entity route` nonzero,
   or a task dropped.
P3 MOVEMENT (HIGH): all 3 taskees move measurably from their birth point toward their route end -
   the entity vertex-by-vertex, the two units via per-subordinate offset routes (G2). Falsifier:
   a taskee static at its birth point 120 s after its task (its members' positions unchanged).
   Note Y-13: armour is OFF-ROAD by default; the PATH will differ from any 5.0.2 road-preferring
   golden - that is EXPECTED, not a miss. Do not score against 5.0.2 path/timing.
P4 COMPLETION (MEDIUM): each task yields a DtTaskCompleteReport (success/taskId/tracking), unit
   completion = ALL subordinates (G3), routed into a TASKCMPLT with rule-4 pairing. Arrival scored
   at the SHIPPED tolerance (at-distance 1.0 m move-along / 2.0 m maneuver; near 15 m), NOT 250 m.
   Falsifier: a taskee that moved but never completes within the window (record as INCOMPLETE - a
   real finding about 5.2 completion, not necessarily a defect); a completion with no movement
   (vacuous - the FALSE-TASKCMPLT the native-completion item exists to catch).
P5 FORMATION WAIT (record, G1): expect a brief hold before a unit moves (formation validity);
   score the outcome (it does move), not a duration. The aggregate's own published Z: recorded.
SUCCESS = P1 AND P2 AND P3. P4 completion is scored and reported; an INCOMPLETE with real movement
is a finding to carry, not a run failure, given 5.2's all-subordinates completion is new to us.

## 4. Competing hypotheses
H-a "movement is really the 5.0.2 road path" - killed by Y-13 (off-road default) + the trace: an
off-road straight-ish track, not road-snapped. H-b "a unit's aggregate Z moved but members did
not" - killed by scoring MEMBERS (disaggregatedActuator derives the unit Z from them). H-c "task
completed vacuously (no move)" - the P4 movement+completion pairing catches it. H-d "the order beat
creation" - the STEP 6b gate + P1 catch it.

# AMENDMENT B - 2026-09-05 - cold-start NO-GO fixes (BEFORE launch)

A cold-start review returned NO-GO; all findings verified by the seat against source. B supersedes
the items it names.

B1 (F1, BLOCKING - the run's real variable, now set). `Vrf:AggregateFormation` defaults to ""
= OFF, which FREEZES disaggregated aggregates on the unresolvable "column-left" default; the
create-time formation repair (query -> set -> REORGANIZE, which fills the promotion map that
formationState.h:52-53 tests for validity) is gated on =="auto" (VrfC2SimService.cs:1342, :1577).
The order file's own header MANDATES it ("Run with Vrf__AggregateFormation=auto"). This is not a
coin-flip: auto is the production, R5-verified (3/3) arm for any aggregate-bearing order. **STEP 6
MUST export `$env:Vrf__AggregateFormation='auto'`.** Without it the entity would move and both
aggregates freeze - an uninterpretable 1-of-3. Placement did not need it (nothing was tasked).

B2 (F2). With auto set, aggregate P3 confidence stands (the formation is repaired + reorganized at
creation, not left to the VRF-8968 wait alone). If a future control runs auto=OFF, drop aggregate
P3 to MEDIUM and score a freeze as a finding, not a failure.

B3 (F3). P2's "99-char route names" is WRONG for this order - route name is `<TaskName> ROUTE`
(~14 chars, VrfC2SimService.cs:1378), carried over from COA-STP1. The route-by-uuid half is
correct (task addressed by the route's real uuid, :1610-1615). Score route-by-uuid, drop the
length claim.

B4 (F4). P1's falsifier SPLITS: DROPPING TASK = _vrfUuidByName miss = order beat creation
(:1070-1076) -> widen the create-before-task gate. ABANDONING TASK = TryGetEntityGeodetic null on a
disaggregated aggregate (:1174-1180, facade cast risk, mitigated VrfFacade.cpp:1018-1027 but FIRST
exercised through the app on 5.2 here) -> a distinct live watch item, NOT a create-gate symptom.
Both must be 0 for P1, for different reasons.

B5 (F5). P3/P4 are scored by tools/analysis/movement_check.py (being built in the same rite);
the run may CAPTURE (WatchVrf 1 s trace + Debug app log) before it exists, but nothing is SCORED
until the scorer is present and self-tested (empty-trace FAIL, 5.2 uuid->name labels).

B6 (F7). "all 6 taskees" -> THREE taskees, SIX created units (1141/1142/1143 created-not-tasked);
the STEP 6b gate confirms all SIX are created (so _vrfUuidByName holds the three taskees) before
PushOrder. OrderID is fixed (a5000000-...); PushInit does ResetToInitializing first, and STOMP is
a live topic (no retained queue), so a stale order cannot pre-fire; the DROPPING guard backstops.

## 5. Result - 2026-09-05, run 20260905T214240Z_run (appNos 3926-3932), VERDICT: PARTIAL - entity + platoon move-along work; the COMPANY (higher-unit) move-along path is BROKEN

CORRECTED 2026-09-05 after the F-DIVERGE research workflow (5 angles + 2 refuters,
wf_52b70722). My first read of this run CLAIMED "SUCCESS (P1 AND P2 AND P3)" - that was
OVER-CLAIMED and is RETRACTED here, along with three wrong facts (below). This is the honest
verdict; the initial one is not preserved because it was refuted, per the discipline of
demoting refuted content the moment it is refuted.

FIRST C2SIM ORDER TO DRIVE MOVEMENT ON 5.2. Runner RunC2SimScenario.ps1 -VrfProfile 5.2
-NoGui -Scenario R9_Mojave_Empty_52, Vrf__AggregateFormation=auto (B1), -RunSecs 360. Env
clean: RTI serviceable (stage 2c), oracle gate PASSED (88 real-coord POS / 44 uuids - NO
wedge), clean teardown, RTI preserved.

- P1 CREATION (HIGH) **PASS**: 6 units created, DROPPING=0, ABANDONING=0, alt-fail=0 (no
  10000, no buried), 1.BdeHQ at terrain+1 m. The 3 UNTASKED platoons stayed still (disp=0)
  - movement is SELECTIVE to tasked units (kills H-a create-settling).
- P2 DISPATCH (HIGH) **PASS**: 3 tasks issued MoveAlongRoute by route uuid; route names
  "T_R5_xxx ROUTE" ~14 chars (B3). No "Can't find entity route".
- P3 MOVEMENT (HIGH) **2/3 - entity + platoon PASS, COMPANY FAIL**: 1.BdeHQ (entity) 1155 m
  and 1222.MechPlt (Tank Platoon) 1150 m advanced along their DUE-NORTH routes and members
  moved with them. **114.MechCoy (Tank Company) did NOT advance toward its route end**: of
  22 members, 18 drove exactly due SOUTH (~412 m, bearing 180) to a backward-extended route
  start and STALLED, 2 ran away NNW (13.7 km / 2.05 km, still moving at close), 0 advanced
  north; centroid 304 m WNW. P3's PREDICTION (all move toward the route end) FAILED for the
  Company; its coarse falsifier ("static at birth") did not fire only because members DID
  move (the wrong way) - a FALSIFIER/INSTRUMENT GAP now recorded (see below).
- P4 COMPLETION (MEDIUM) **2/3**: 1.BdeHQ + 1222.MechPlt reported non-vacuous TASKCMPLT
  (both advanced -> kills H-c). 114.MechCoy never completed (the Company path has NO
  stuck/exhausted fail-safe, so a still-moving runaway blocks all-subordinates completion).
- P5 FORMATION (record) **PASS**: B1 auto set+reorganize fired for all 5 aggregates at
  create (verified live). Did NOT prevent the Company failure.

VERDICT = PARTIAL. C2SIM MoveAlongRoute works end-to-end on 5.2 for the ENTITY and PLATOON
echelons (advance + non-vacuous completion). The COMPANY (higher-unit) move-along path is
BROKEN. The claim "tasking drives movement on 5.2" holds ONLY for entity + platoon here.

### THREE CORRECTED FACTS (were wrong in the first read; all re-verified from primary files)
1. Route T_R5_CO1 is DUE NORTH (constant longitude -116.693388, lat increasing;
   R9_Mojave_UnitMove_Order.xml:49-59) - NOT "S/E".
2. Both divergers went NNW nearly PARALLEL (~327-331 deg = the north route rotated ~29 deg
   CCW) - NOT "one NW, one NE". A shared rotation, not two independent glitches.
3. The Company failure is UNIT-WIDE (18 south-stall + 2 runaway + 0 advance) - NOT a lone
   runaway on an otherwise-good move. "114.MechCoy moved 304 m" was a broken-move centroid.

### F-DIVERGE diagnosis (MEDIUM) and the falsifier/instrument gaps
DIAGNOSIS: the Company higher-unit move-along wiring is a DISTINCT controller -
aggregate-move-along-controller with NO adapter and NO maneuver-along-controller and NO
isUnitMovementExhausted fail-safe (ground-higherUnit-disaggregated-movement.sysdef:177-203)
vs the Platoon's adapter + maneuver-along (ground-disaggregated-movement.sysdef:176-221);
only the Company failed. That distinct no-fail-safe path is the SUBSTRATE/amplifier of
non-completion (VERIFIED, both refuters). The DIVERGENCE itself is BETTER explained by an
UNRESOLVED/TRANSIENT aggregate formation-ORIENTATION at task time rotating per-subordinate
body-frame offsets ~29 deg to a common NNW bearing (both refuters WEAKENED the pure
code-path reading -> this orientation reading); likely TRIGGER = the STACKED zero-spread
co-located birth of 114.MechCoy + siblings 1141/1142/1143 at one identical point
(vrfc2simapp.log:41-53; DeStackCreates OFF), vs the platoon born alone. OPEN ANOMALY (not
footnoted, falsifier for BOTH readings): divergers sustained ~35-43 m/s at timeMult=1, above
M1A2 governed off-road speed and above the 1.5x formation catch-up cap - unaccounted.
INSTRUMENT GAP: movement_check.py scored the Company MOVED/PASS on displacement MAGNITUDE; it
does not test progress TOWARD the route end, so it laundered a broken wrong-way move as PASS.
Redesign tracked (add per-taskee progress-toward-route-end + runaway flag). Full record and
the decisive next probe: docs/experiments/PREREG_F_DIVERGE_2026-09-05.md.
